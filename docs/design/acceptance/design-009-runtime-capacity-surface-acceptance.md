# ACC-RUNTIME-001 · 执行工位产能判定面 验收记录

> 状态: 权威当前
> 最后核对: 2026-08-31
> 适用范围: `DESIGN-009` 交付的 `/collection/runtime` 及 2026-08-31 共享采集状态硬化
> 事实来源: 真实 loopback 运行结果、真实 Chrome 实拍与 DOM 实测、`cargo test -p linggan-api`
> 冲突时以谁为准: 真实运行/代码/合同、用户最新确认

## 1. 验收对象

- 事项: `DESIGN-009`（无 Issue，Mog 直接指定）
- 页面: `/collection/runtime`
- 关联 ID: `PAGE-COLLECTION-001`、`LIDS-SYS-001`、`LIDS-PAT-001`、`DECISION-04`、`INV-36`
- 环境: 2026-08-29，macOS Chrome 经 DevTools 协议驱动；**新版跑在 3100 端口的独立进程**，`com.linggan-intelligence.local-runtime`（3000，冻结快照 a6338df）未被触碰
- 数据前提: 1 台已登记工位（在岗，插件 0.5.2）、1 个未归位安装（插件 0.5.0）、2 个观察目标（1 个开着巡检）、当日已入库 20 篇、无风险暂停、无活跃租约

## 2. 场景矩阵

| 场景 | 用户任务 | 预期含义 | 结果 |
|---|---|---|---|
| 三秒答案 | 判断系统能不能接活 | 首屏给出判断词而非一串读数 | 通过（「能接活」） |
| 缺哪一样 | 接不了时知道去修什么 | 逐条说明是哪条 lane、缺什么 | 通过（单测覆盖四种 `Capacity`） |
| 两条 lane 分开判定 | 深度建档与巡检状态不同 | 不合并成一句「资源不足」 | 通过（「部分接不了活」） |
| 账号未参与判定 | 知道第 5 问有一项没被检查 | 显式标注，不装作四项齐备 | 通过（`c-factor-unchecked`） |
| 接通状态实读 | 页面不说过时的假话 | 五处写死状态全部由事实推出 | 通过（`the_page_never_writes_a_connection_state_by_hand`） |
| 读不到 ≠ 没有 | 数据库读不到时 | 说「读不到」，不说「接不了活」 | 通过 |
| 调度静默的两种原因 | 区分「在跑但没活派」与「没在跑」 | 两者分开表述，且不声称读得到心跳 | 通过 |
| 从未派出 | 判断是否跑过 | 显示「从未」，不是 0、不是未知 | 通过 |
| 重装不产生僵尸工位 | 5 次重装仍是一台工位 | 计数可见，行数为 1 | 通过（「换过 5 次插件」） |
| 动作失败可见 | 登记重名 | 失败条出现在表单上方并说明原因 | 通过 |
| 说明句不重复 | — | 「工位 vs 安装」全页只出现一次 | 通过（断言计数 == 1） |

## 3. 视觉工作条件

- 视口: 1920×1080 真实 Chrome 实拍；1280×800 DOM 实测
- 几何: 128px 全局页头、216px 导轨、右栏 400px（≤1450px 收至 340px，≤1180px 落为单栏）
- Token 依据: 页面样式表内 `#` 计数为 **0**
- 重音预算（LIDS 上限 8）: 品牌标识 1 + 导轨当前项 1 + 登记主按钮 1 + 「未归位」琥珀 1 = **4**
- 排版: 功能文字最小 10px（仅 Mono 刻度类标签），阅读文字 ≥11px，正文 13px
- 动效: 仅主按钮按下位移，`prefers-reduced-motion` 下关闭（沿用既有规则）

### 1280×800 的横向溢出

页头在 1280 下溢出，**这是既有缺陷**：`ACC-COLLECTION-001` §5 已明确「未覆盖 1280×800」。对照实测：

| 指标 | 旧版 (3000) | 新版 (3100) |
|---|---|---|
| `.v7-global-header` 宽 | 1352 | **1329** |
| `.v7-context-meta` 宽 | 756 | **632** |
| 上下文行信息量 | 2 读数 + 3 状态词 | 2 读数 + 2 状态词（信息更多，词更少） |

本次未修复该溢出（属外壳几何，超出本事项范围），但**没有加重它**。

## 4. 分层结论

| 完成层 | 结论 | 证据 | 仍有限制 |
|---|---|---|---|
| 任务是否成立 | NOT VERIFIED | 待 Mog 在浏览器确认 | 这是本次唯一等人的一层 |
| 状态是否诚实 | VERIFIED | 15 条新单测，逐条锁住一种说谎方式 | 调度心跳无记录，页面如实说读不到 |
| 视觉是否符合 LIDS | VERIFIED | 1920 实拍；色值 0；重音 4/8 | 未覆盖 1440/1536/1728/2560/390 |
| 动作是否有真实回执 | VERIFIED | 本次未改动任何动作；表单失败态单测覆盖 | — |
| 自动检查 | VERIFIED | `cargo test -p linggan-api` 57 passed；fmt、clippy 全绿 | 见下方既有失败 |
| 真实链路 | N/A | 本次无任何写入或外部请求 | — |

### 既有失败（非本次引入）

`base_no_db_header_and_nested_technical_keys_remain_chinese_first` 与 `evidence_library_uses_chinese_for_user_meaning_and_english_only_as_technical_keys` 两条**在改动前的 main 上就是红的**，已用 `git stash` 对照基线确认（基线 47 passed / 2 failed，本次 57 passed / 2 failed，失败项相同）。属语料页，未在本次范围内修复。

## 5. 待 Mog 裁定

1. **页面主问题变更**：`PAGE-COLLECTION-001` §2 的「执行工位、队列、租约与回执是否健康？」已被实现改为「有没有能力接活」。规格文件尚未同步——是承认变更并改规格，还是回退实现？
2. **今日额度用满 200/200 时那台工位显示成什么**：「在岗」不够诚实（它还在岗，但今天不会再干活了）。这是状态语义，本次按现状渲染，未自作主张加状态。
3. **是否新增调度心跳**：页面现在如实说「读不到调度进程的心跳」。要让它答得出，需要一张心跳表 + worker 写入——属新功能，本次未做。

## 6. 明确未证明

- 调度进程此刻是否活着（系统无心跳记录）。
- 真实采集能力、账号资格（`DECISION-04` 独立立项，第 5 问的账号那一半从未被检查）。
- 1440×900、1536×960、1728×1117、2560×1440、390×844 视口。
- 完整 Tab 顺序与焦点环覆盖。
- 其余四个采集子面的接通状态仍为静态文案（`DESIGN-009-UI-EX-01`）。

## 7. 2026-08-31 共享状态硬化复验

### 7.1 旧例外的关闭条件已经出现

DESIGN-009 初次验收时，系统确实没有 scheduler heartbeat，因此第 5 节把“是否新增调度心跳”留给 Mog 裁定，并允许其余四个采集子面继续静态文案。之后 `0020_observation_runtime_automation.sql`、常驻 worker 与 `read_scheduler_heartbeat` 已进入主线和本机运行；旧例外继续存在反而使页面与真实数据库冲突。本次不新增心跳能力，只把已经存在的事实接回所有相关表面，关闭 `DESIGN-009-UI-EX-01` 的代码级缺口。

### 7.2 状态矩阵

| 表面 | 已知事实 | 新表达 | 失败/未知表达 |
|---|---|---|---|
| 观察目标页 | total / monitoring / archiving | 真实计数；采集导航“观察中”或“无观察目标” | 单项读不到显示 `UNKNOWN`，不显示 0 |
| 生产流 | scheduler heartbeat | “调度运行中”或“调度心跳已过期” | heartbeat 不可读时才显示“调度心跳读不到”；语义事件读模型未接入时明确 `NO EVENT READ MODEL` |
| 待处理 / 采集任务 | 目标计数与共享运行状态 | 有目标时不再声称“没有事项/任务” | 对应 read model 未接入时显示“当前未知” |
| 执行工位 | capacity / station roster / heartbeat | 空缺工位、未归位安装与调度状态分别实读 | 每项独立未知，不连坐其他事实 |
| Corpus 一级导航 | 观察目标总数 | “观察中 / 无观察目标 / 状态未知” | 读取失败必须显示“状态未知”，不得推断已接通或为零 |

### 7.3 自动与真实只读证明

- `cargo test -p linggan-api`: **58 passed / 14 ignored / 0 failed**；ignored 项都明确要求隔离 PostgreSQL proof harness，不是本次失败。
- `./scripts/test-local-001-discovery-postgres.sh`: 隔离 PostgreSQL 全链 **46 passed / 0 failed**，覆盖 9 个 local discovery、6 个 local producer、6 个 material projection、5 个 social、6 个 media 与 14 个 API 测试。
- 新增回归锁住 running、stale、unreadable 三种 heartbeat；2 个目标 / 1 个巡检 / 1 个建档；生产流第二栏；Corpus 共享导航；以及“有目标时不得同时显示暂无目标/调度未接通”。
- 候选 API 在 `127.0.0.1:3101` 连接本机持久数据库做只读验证，3000 正式进程未被替换：`/health` 为 scheduler running；目标页显示巡检 1、建档 1；执行工位显示空缺 0、未归位 1；Corpus 显示采集“观察中”。目标页与生产流均不再出现“调度器未接通”，目标页不再出现“暂无观察目标”。
- 全依赖严格 Clippy 被 `linggan-evidence` 既有 11 个告警阻断，位置均不属于本次 diff；不能宣称 workspace Clippy 全绿。本次包级检查、治理检查和 diff 检查在提交前继续执行。

### 7.4 尚未证明

- 当前候选尚未合入 `main`，也没有替换 3000 端口的冻结运行快照。
- 尚未在发布后的真实 Chrome 中完成多视口视觉与交互复验。
- 待处理、采集任务与生产流的业务列表 read model 仍未实现；页面只修正为“未知/未接入”，没有伪造这些功能已经具备。
- 本次没有新增或执行任何平台采集、没有改变观察目标、工位、授权、任务或 Receipt。
