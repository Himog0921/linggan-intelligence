# RUNTIME-STATION-V7-2-001 · 执行工位页内容区按 v7.2 稿复刻 + 运行概览抽屉

> 状态: 权威当前
> 最后核对: 2026-09-20
> 适用范围: `/collection/runtime`（执行工位）页面的内容区结构、仪器面表达、按钮区与右侧运行概览抽屉
> 事实来源: Mog 于 2026-09-20 提供的 `linggan-execution-station-v7-2-runtime-drawer.html`、Mog 同日的两项范围裁定与一项数据裁定（见 §1）、Mog 同日对**控制条**的四项追加指示（见 §5c）、[design-governance.md](../design-governance.md)、[../../agents/ui-execution-contract.md](../../agents/ui-execution-contract.md)、[../lids/patterns.md](../lids/patterns.md) Collection Control、[../lids/language-policy.md](../lids/language-policy.md) LANG-05、[../lids/data-boundaries.md](../lids/data-boundaries.md)、[../lids/materials.md](../lids/materials.md)、[../lids/decisions.md](../lids/decisions.md)
> 冲突时以谁为准: 用户最新确认、真实数据合同与运行事实；本清单不改写任何准入判定、权限、账号资格或工单口径

## 1. 事项

- Issue / SCOPE：[Issue #309](https://github.com/Himog0921/linggan-intelligence/issues/309)（Mog 于 2026-09-20 直接指派）
- Agent 与 worktree：`feat/runtime-station-v7-2-001` @ `.worktrees/runtime-station-v7-2-001`，基线 `origin/main@5371311`
- 目标：把 v7.2 设计稿的**内容区**复刻到线上执行工位页，并新增右侧「运行概览」抽屉

### 用户可见结果

1. 内容区顶部出现一条**控制条**：实时标记 + 工位控制 + 四个读数（接单状态 / 在岗工位 / 正在执行 / 自动排程）+ 三个动作（新增工位 / 运行概览 / 去观察目标页）。它是一条**夹在上下两条线之间的跟栏**，不是一张卡片（见 §4b、§5d）；
2. 「能不能接活」由一段散文改写为**接单仪器面**：深色仪器表头（阻塞数徽章 + 判定 + 通道原因码）+ 左侧逐条通道状态 + 右侧四个读数（在岗工位 / 今日预算 / 风险余量 / 观察账号）；
3. 工位表、今日运行、自动观察排程三块按 v7.2 的仪器语言重绘（区段徽章与序号、量化额度轨、能力行内签、读数格、排程状态列）；
4. 右侧新增**运行概览抽屉**：贴边标签页，点开从右侧滑入、Esc 关闭、窄屏默认收起；抽屉内四块内容全部来自真实读模型。

### Mog 于 2026-09-20 的三项裁定

| 裁定 | 内容 |
|---|---|
| 改动范围 | **只复刻内容区**。左侧导航与顶栏（共享外壳）保持现状，其它页面完全不受影响 |
| 英文标签 | **按现有规则只留中文**。稿中的 `WORKSTATION LIST`、`TODAY'S OPERATIONS`、`OBSERVATION SCHEDULE`、`RUNTIME OVERVIEW`、`LIVE`、`SUCCESS`、`FAIL`、`TOP 3 · 24H`、`ALL ONLINE` 等描述性英文一律不加（LIDS-LANG-05） |
| 抽屉内容 | 稿中三块内容在本系统**没有真实来源** → **换成真读得到的四块**，稿中数值一个都不出现 |

### 明确非目标

- 不改左侧导航与顶栏。二者由 `shell.rs` / `shell.css` 为所有页面渲染，ADR-10（shell 区域冻结）与 `patterns.md` 的所有权规则都要求页面样式表不得声明它们；测试 `no_page_stylesheet_restyles_a_component_the_shell_owns` 已在强制这一点；
- 不改观察目标页、采集任务页、待处理页、生产流页、证据库；
- 不新增任何写动作、字段、权限、数据口径或后端查询。所有既有 POST 地址、字段名与错误码逐字不变；
- 不引入 v7 token 值到运行时（ADR-P02）。本页只消费既有 `--lgi-*` 与页内已登记的 `--c-*` / `--v7-*` 别名；
- 不新增材料、场景、动效或深色主题（ADR-07）。深色仪器面只有两处：接单仪器表头与抽屉执行结果卡，且各自只使用已批准的深色面语法。

## 2. 读取回执

| 来源 | 状态 | 本次解决的问题 | 已核对 |
|---|---|---|---|
| `CLAUDE.md` / `docs/current-state.md` | 已读 | 确认交付分级、worktree 唯一位置、提交前独立审核与「文档先于实现」四条硬要求 | 是 |
| UI execution contract | 已读 | 本次为**混合**变更（展示 + 交互 + 状态语义），按最高风险类别收集依据；实施前建立表面地图、状态词典、依赖地图与验收矩阵 | 是 |
| `docs/design/lids/README.md` | 已读 | 确认 LIDS 权威边界与「已采纳 ≠ 现有代码已这样」的两栏读法；确认「没有权威依据的部分必须停止并标记 DECISION_REQUIRED」 | 是 |
| `docs/design/lids/tokens.md` | 已读 | §2 v7 三层目标值与稿中调色板一致，但 §3 运行时基线仍是 `--lgi-*`；§4 迁移触发条件未满足 → 本次不新增 token、不改 token 值 | 是 |
| `docs/design/lids/decisions.md` | 已读 | ADR-02 字重三档、ADR-03 字号下限 11px、ADR-04 信号拆分、ADR-07 无暗色模式、ADR-09 量化淡出、ADR-10 shell 冻结、ADR-11 文本标签页、ADR-P02 运行时 token | 是 |
| `docs/design/lids/patterns.md` | 已读 | 采集运行域主 Pattern = `Collection Control`；shell 所有权规则；页头收回规则（无可见标题块、`h1` 仅 `.v7-sr-only`） | 是 |
| `docs/design/lids/materials.md` | 已读 | 材料预算 70/20/10；M-05 只用于局部深色面；一个容器最多一种材料；纹理不替代状态 | 是 |
| `docs/design/lids/language-policy.md` | 已读 | LANG-05 Mono 预算三类白名单：机器事实、封闭集合的系统状态枚举、结构编号；描述性标签禁止英文 | 是 |
| `changes/runtime-station-table-001-ui-change-manifest.md` | 已读 | 上一轮（2026-09-13）把这一页定为「以工位为单位的管理台」，本轮在其之上**只改表达、不改判定与结构语义** | 是 |
| `changes/design-011-collection-surfaces-v7-ui-change-manifest.md` | 已读 | 前一轮的 v7 落地范围与欠账（共享 shell 未动、ADR-04 信号拆分受阻） | 是 |
| 数据合同：`RuntimeCapacityOverview` / `StationOverview` / `StationCapability` / `UnclaimedInstallation` / `RuntimeResourceView` / `RuntimeLaneControlView` | 已读 | 逐字段核对抽屉四块内容的真实来源与可空性（见 §4 依赖地图） | 是 |
| 真实代码：`station_view.rs`（生产段 1–1525 行）、`collection_workspace.css`、`collection_workspace.js`、`shell.rs`、`lids_tokens.css` | 已读 | 现有 6 个渲染函数、40 条断言、可复用的 `--lgi-*` / `--c-*` / `--v7-*` 取值、shell 侧不得触碰的类名清单 | 是 |
| 运行页实测 | 已读 | 2026-09-20 抓取 `http://localhost:3000/collection/runtime` 渲染产物，逐条核对现有可见文字与结构 | 是 |

### 2b. 稿中数值的真实性核查（本清单的关键证据）

在写任何代码前，逐块核查了 v7.2 稿抽屉中四块内容在本系统是否有真实来源：

| 稿中内容 | 核查结论 | 证据 |
|---|---|---|
| 规则策略 3/3 正常 | **有真实来源** | `RuntimeLaneControlView` → `RuntimeControl.lanes`，三条通道各有中文名与可接活判定 |
| 回传成功率 98.6% / `+2.1` / 12 根柱 / `1,842 SUCCESS` / `26 FAIL` | **无此读模型** | 全仓检索 `回传`/`成功率`/`receipt_rate`/`success_rate` 无运行时读模型；仅 `StationCapability.successes` 与 `capability_failures`/`execution_failures` 存在，且口径是**近 7 天、按能力分**，不是 24 小时全局，也没有逐时序列可画柱 |
| 补采入口「失败任务列表 26 条」「手动触发补采」 | **运行页无此动作** | `apps/api/src/local_web.rs` 的 `/collection/runtime` 8 条 POST 全为本地登记类动作，无补采；`补采` 的真实入口在 `/collection/targets`（目标级），不在本页 |
| 失败原因 `插件无响应 62%` / `目标页面结构变化 24%` / `账号未绑定 14%` | **无聚合读模型** | 全仓 `failure_code` 只有三个取值（`page_read_failed`、`capability_not_executable_here`、`lease_expired`），且没有任何按原因汇总的查询或读模型 |

**结论**：稿中这三块数值在本系统中不存在对应事实。照搬即为伪造运行事实，直接违反 `data-boundaries.md` 与 `CLAUDE.md`「mock 与编译通过不能证明真实链路成功」。Mog 于 2026-09-20 裁定「换成真读得到的四块」，§4 的依赖地图给出替代来源。

## 3. 变更分类

- 分类：**混合**（展示 + 交互 + 状态语义）
- 最高风险类别：**状态语义**
- 对应来源 ID：`LIDS-PAT-001`（Collection Control）、`LIDS-LANG-001` LANG-05、`LIDS-MAT-001` 材料预算、`LIDS-BOUND-001` 四态、`LIDS-PRI-001` 排版与按钮、`LIDS-ADR-010` shell 冻结
- 为什么该类别足以覆盖本次风险：本次**不新增任何事实来源**，只改变同一批已有的真实事实的**表达与归位**。四块新内容全部来自本页已经在读的读模型（见 §4），没有新增 SQL、没有新增字段、没有新增写路径。三处易被"顺手改语义"的地方已单独设卡：
  1. **「接不了活」的判定口径不变**——仍由 `lane_rows` 决定，本轮只换它的外壳；
  2. **量化额度轨不替代数字**——轨只是 55/200 的量级读法，精确数字始终在它上方；
  3. **深色面只承载真实读数**——不出现没有数据来源的柱、趋势或百分比。
- 是否存在 DECISION_REQUIRED：**否**。三项方向性裁定已由 Mog 于 2026-09-20 当面拍板（§1）。
- L1 / L2 / L3 与主 Pattern：L1 · `Collection Control`（`Header / Active Workstations / Risk / Next Window` → 状态矩阵 → 队列与详情）
- 是否触及 Token、Primitive、CMP、Scene、Motion 或 Data Truth：
  - **Token**：不新增、不改值。新增样式只消费既有 `--lgi-*` 与页内已登记的 `--c-*`（`DESIGN-010-UI-EX-01` 例外块）；
  - **Primitive / CMP**：复用既有按钮语法（`.c-btn-primary` / `.c-btn-quiet`）、宽表栅格（`.c-tg-*`）与区段语法，不新建组件规格；
  - **Scene**：不涉及；
  - **Motion**：只新增抽屉的开合位移（260ms）与标签页淡出，两者均受 `prefers-reduced-motion: reduce` 关闭；不使用循环动画，不新增扫描带动效；
  - **Data Truth**：不改变任何一轴。四态（读得到 / 读不到 / 没有 / 未知）的既有区分全部原样保留。

## 4. 影响边界

### 4a. 表面地图（改动落在哪些表面）

| 表面 | 现状 | 本轮变化 |
|---|---|---|
| 共享外壳（顶栏 / 上下文行 / 左侧导航） | 由 `shell.rs` + `shell.css` 拥有 | **不动**。页面样式表不得声明 `.v7-global-*` / `.v7-primary-nav` / `.v7-nav-*` / `.v7-context-*` / `.v7-side*` / `.v7-app` / `.v7-shell` |
| 内容区 · 控制条 | **不存在** | **新增**（`.c-deck`）：实时标记 + 名称 + 4 读数 + 3 个动作。**上下两条实线夹出的一条跟栏**，左右不加边框、整条不落阴影；行内按钮用**标准按钮语言**（与「登记」同款，含硬影）。开抽屉的入口就在这一行里（见 §4b、§5d） |
| 内容区 · 接单判定 | `.c-verdict` 散文块 + 通道行 + 因子格 | 重绘为 `.c-instr` 仪器面：深色表头（含右侧采样点阵与 `INSTALLATION_STATE`）+ 左侧通道状态列 + 右侧 4 读数格**并排一行**（见 §5d）。判定文案与通道数不变 |
| 内容区 · 工位表 | `.c-stn` + `<details>` 11 列宽表 | 重绘为区段语法（徽章 + `h2` + 右侧 meta）+ 行内量化额度轨 + 能力签。**可展开的管理面板与全部动作原样保留** |
| 内容区 · 今日运行 | `.c-run` + `dl.c-run-readouts` 6 格 | 重绘为 `.c-ops` 六格读数条（标题 + 编号 + 值 + 说明）。**去掉稿中的装饰性柱状图**（见 4d） |
| 内容区 · 自动观察排程 | `.c-run` + 6 列宽表 | 重绘为区段语法 + 9 列（新增「序」「状态」，最后列由稿中的 `⋮` 改为真实可点的入口） |
| 内容区 · 登记与未归位安装 | `.c-stn-footer` | 未归位安装保留在页尾；**登记表单从页尾搬进一个居中弹窗**，由控制条的「新增工位」打开（见 §5d）。POST 的 method / action / 字段名 / 错误码映射逐字不变 |
| 右侧 · 运行概览抽屉 | **不存在** | **新增**：贴边标签页 + 固定抽屉 + 4 个内容块 |
| 共享脚本 | `collection_workspace.js` | 新增抽屉开合（含 Esc 与窄屏默认收起）；不改动既有行为 |

### 4b. 状态词典（本轮出现或保留的用户可读状态）

| 状态词 | 来源 | 语气 | 不可与什么互换 |
|---|---|---|---|
| 能接活 / 部分接不了活 / 接不了活 / 读不到 | `lane_rows` 汇总 | 成功 / 警告 / 危险 / 中性 | 「读不到」不等于「接不了」 |
| 可接活 / 可排队，等工位 / 接不了 | 单条通道 | 成功 / 警告 / 危险 | 「可排队」既不是「可接活」也不是「坏了」 |
| 在岗 / 失联 / 空缺 | `station_state`，阈值直接引用 `CONTROL_FRESHNESS_MINUTES` | 成功 / 警告 / 中性 | 「失联」不等于「空缺」，也不等于「坏了」 |
| 自动 / 已暂停 / 读不到 | `accepting_cell` | 成功 / 警告 / 中性 | 「读不到」不写成「已暂停」 |
| 已确认 / 待确认 / 已变化 / 缺资格信号 / 未绑定 | `account_binding_label` | 成功 / 警告 / 警告 / 警告 / 中性 | 「未绑定」不是警告，是尚未指派 |
| N 就绪 · N 降级 · N 未验证 | `capability_counts` | 降级决定语气 | 「未验证」不等于「降级」 |
| 从未 / — | `moment_or_never` / `short_ref` | 中性 | 「从未」不是 0，也不是未知 |
| 已完成 / 执行中 / 等待领取 / 已取消 / 历史状态 / 还没排过 | `work_order_state_label` | 中性 | 封闭集合的系统状态枚举，原样保留 |

### 4c. 依赖地图（抽屉四块内容的真实来源）

| 抽屉内容块 | 真实来源字段 | 无数据时的表达 |
|---|---|---|
| **规则策略** | `RuntimeControl.lanes`（三通道中文名 + `available`/`queueable` + 原因码），读不到时退回 `RuntimeCapacityOverview.lanes` | 整块显示「读不到」，不写「正常运行」 |
| **执行结果（近 7 天）** | `StationCapability.successes` / `capability_failures` / `execution_failures` 跨台求和；最近一次拿回取 `PatrolOutlook.last_succeeded_at` | 分母为 0 时显示「尚无记录」，不显示 0% |
| **补采与失败入口** | 真实路由 `/collection/tasks`、`/collection/targets` | 不显示任何读不到的计数；两个入口是导航，不是动作 |
| **积压与重试** | `DispatchLaneBacklog` 的 `queued_work_orders` / `leased_work_orders` / `retry_cooling_work_orders`；`ActiveRiskPause` 列表 | 整块读不到时写整句「这一轮没有读到队列事实，因此答不出积压与重试的现状」，不写 0；队列读得到、但没有在等的单时写「没有在等的一单」。两种情形不合并 |

页脚：`PatrolOutlook.last_dispatched_at`（上次派出时间），空值显示「从未」。

### 4d. 对稿的有意偏离（逐条留痕）

| 稿中做法 | 本轮做法 | 理由 |
|---|---|---|
| 抽屉「回传成功率 98.6%」及 12 根柱状图 | 改为「执行结果 · 近 7 天」的真实成功与失败计数，**不画柱** | 无 24 小时全局读模型，也无逐时序列；柱状图会读作真实趋势 |
| 抽屉「补采入口」两个动作按钮 | 改为两个真实导航入口，**不显示「26 条待检查」** | 该动作与计数在本页都不存在；不造不存在的动作 |
| 抽屉「失败原因 TOP 3」 | 整块换成「积压与重试」 | 全库只有三个 `failure_code` 且无聚合读模型 |
| 控制条第 3 个按钮「查看观察轨迹」 | 改链真实存在的观察目标页 | 一级导航中没有「观察轨迹」这一项，原按钮在本系统没有落点，不能做成死链 |
| 开抽屉的入口**位置**：贴页边那片的固定标签 | 入口改为控制条内的一个按钮（`运行概览`），贴边标签整块删除 | 第二轮删掉重复的那一处时，留错了另一半：留下的贴边标签虽然固定可达，却悬在内容区的两条线之外，读起来不像这一行的动作。用户的裁定是「运行概览的按钮要在两个线条内」。代价如实记录：按钮会随正文滚走，滚到页面下方时够不着——这也是当初留标签的理由，由用户确认接受 |
| 今日运行六格下的装饰性柱状图 | **不做** | 六格读数没有任何一格带序列数据；无来源的柱违反了「纹理不替代状态」与「不得营造假运行感」 |
| 工位行末的 `⋮` 菜单 | 保留为可点开该行的「明细」（`<summary>`） | 该行的管理面板里是真实动作（更名 / 停用 / 接活开关 / 认领窗口 / 账号确认），不能替换成一个只弹提示的菜单 |
| 排程表末列 `⋮` | 改为链到观察目标页的真实入口 | 本页没有逐规则动作；链接指向真正能操作规则的那一页 |
| 深色「传感器」斜纹与点阵 | 只用 `--lgi-mosaic-on-dark` 已登记的深色面语言，覆盖率受限 | `materials.md`：M-05 只用于局部深色面，且不得成为页面主视觉 |
| 抽屉默认画成**打开** | **默认关闭**，关闭时带 `inert` | 稿子画的是"打开时是什么样"，不是一个默认状态。覆盖层默认打开会盖住正文，且屏幕外的链接会留在 Tab 序列里 |
| 抽屉面板的柔和光晕 `-18px 0 42px rgba(11,15,18,.12)` | 硬边阴影 `-4px 4px 0 var(--v7-black)` | 这套语言里所有浮起的面都是硬边的；柔和的半透明 rgba 光晕在 token 里没有对应值 |
| 把手 42px 高、`top:112px` | 34px 高、顶端贴固定头下沿 | 稿子的把手落在一片 388px 宽的页边空白上（`.control-deck` 只到 1484/1872）。这一页的正文铺满，42px 会压到控制条的动作按钮上——实测交叠约 70×13px |
| 把手自带 `-4px 4px 0 rgba(11,15,18,.18)` | 同款硬边阴影，用 `--v7-black` 实色 | 同上「硬边阴影」一条 |
| 控制条画成一张**卡片**（首版实现：左右上下四边框 + 硬边阴影） | **上下两条实线**夹出的一条跟栏，左右不分边框，整条不落阴影；行内的两个按钮也不落 | 首行站在页首，再给它一层卡片外框和一层投影，它会读成"这一页的主角"——可它只是一条跟栏，主角是下面那张接单仪器面。跟栏的上下两条线就是这一行的边界，按钮的投影会越过下面那条线：刚拆掉的卡片外框，等于又从一个按钮底下长回来 |
| 行内按钮的悬停位移 `translate` | 一并取消（**仅限这一行**；`.c-btn` 在别处照旧） | 那个位移本来就是给投影让位的。投影没了，它只会让按钮在一条对齐的跟栏里斜着跳一下 |
| 控制条四个读数的值**恒定墨色**（首版实现） | 四档各自定义：带状态的那一格，上沿压一条 3px 色带，数值本身换成同一档的墨色 —— 能接活=绿、部分接不了活=橙、接不了活=红、读不到=中性灰 | 稿里那格只有一条二值信号轨；本页的状态从 `verdict_state` 出来就是四档。把四档压回"上色/不上色"，等于把「答不出」和「能接活」画成同一个样子。四档共用同一套形状（色带 + 同色数），只换颜色，所以横着扫过来比的是颜色而不是形状，不会把其中某一档读成"另一个控件" |
| 带状态那格底下**铺一层同色淡底**（照抄稿子） | **不铺**，只留色带与墨色 | 稿里那一格是「色带 + 一层淡底往白色退」。淡底垫在字下面等于把字和底往中间拉，实测把「读不到」从 4.71:1 拉到 4.14:1（15px 粗体按正常文字判要 4.5:1）。两档里 `unknown` 正是被这条规则拉下水的，所以淡底不做。**注**：「部分接不了活」在白底上本身就是 3.88:1，与本条规则无关——问题在 `--lgi-warning` 这个**字角色** token 本身，要动它得先动 token 基线，不在本轮范围内 |
| 「读不到」那档的色带取同族的浅色 `-fill` | 取 `--v7-unknown`（同族的深色） | 四档里 `unknown-fill` 取自 `--lgi-unknown-dot`，是四色中最浅的，本来就是给列表里一个小圆点用的；拉成 3px 横条压在两行黑线之间几乎看不见。一条看不见的定义等于没有定义——那样「读不到」在这一行里就退化成"没有状态" |
| `@media(max-width:900px)` 把控制条折成一列、抽屉铺满 `100vw` | **不做** | 本页正文有 1040px 的最小宽度（`.c-page` 既有规则），再窄浏览器只会显示左侧一段。为不会出现的排版写折叠规则没有验收路径；把抽屉撑到 `100vw` 还会盖住左侧导航 |

### 4e. 其余边界

- **受影响的页面**：仅 `/collection/runtime`
- **受影响的组件**：`apps/api/src/local_web/station_view.rs`（生产段全部渲染函数）、`collection_workspace.css`（执行工位样式段）、`collection_workspace.js`（新增抽屉行为）
- **是否影响数据口径、权限、敏感展示或真实行动**：**不影响**。账号身份摘要仍不上报、凭据仍只显示有效性、8 条 POST 的 method / action / 隐藏字段名 / 错误码映射逐字不变
- **禁止修改的文件/能力**：
  - `crates/` 下的任何判定、读模型与 SQL；
  - `shell.rs` / `shell.css`（共享外壳）；
  - 任何 migration；
  - 任何其它页面的渲染函数与样式段
- **停止条件**：若某一块内容在真实数据里找不到可靠来源，该块显示「读不到」并**整块移除**，不得留空、补默认值或退缩成装饰；若需要新的视觉规则或新 token，停工并升级。

## 5. 验收与证明边界

| 层级 | 验收方法 | 实际结果 | 未证明边界 |
|---|---|---|---|
| 任务可用 | 见 §5b 实拍走查 | 见 §5b | 只在桌面宽度走查 |
| 状态诚实 | `cargo test -p linggan-api --bin linggan-api` 全量 | 见 §5b | 降级、风险暂停、读不到三态用合成夹具验证，非真实故障复现 |
| 视觉一致 | `./scripts/check-project-governance.sh` + 浏览器多档宽度走查 | 见 §5b | 未做跨浏览器与高分屏验收 |
| 真实后果 | 8 条 POST 的 method / action / 隐藏字段名 / 错误码映射与改写前逐字比对 | 见 §5b | 未实际点击提交任何写动作（会改动真实工位记录） |

### 5b. 实施后回填（2026-09-20）

**实施范围**（逐行对应 §4a）

- `station_view.rs`：内容区六个渲染函数改写 + 新增抽屉渲染。`render_runtime` 仍是唯一入口，路由 `/collection/runtime` 与 `local_web.rs` 的任何路由、表单字段、错误码映射均未触碰；
- `collection_workspace.css`：新增 `.c-deck*` / `.c-instr*` / `.c-sect*` / `.c-ops` / `.c-op` / `.c-usage*` / `.c-rdrawer*` 各段，删除被替代的 `.c-verdict*` / `.c-factors*` / `.c-lanes*` 段；
- `collection_workspace.js`：新增抽屉开合 IIFE。

**逐层验收**

| 层级 | 方法 | 实际结果 | 未证明边界 |
|---|---|---|---|
| 任务可用 | 浏览器实拍走查，1600 / 1230 / 1150 三档宽度 | 通过 | 只在本机 Chrome 1600×900 窗口；未跨浏览器、未做高分屏与真实触摸。本行与下面的几何实测由实施者在本会话内用脚本读出，**收尾审核的子代理无浏览器，未独立复现**，只做了算式核对 |
| 状态诚实 | `cargo test -p linggan-api --bin linggan-api` | **240 passed; 0 failed; 21 ignored**，与本轮改动前的基线逐字相同 | 降级 / 风险暂停 / 读不到三态由合成夹具构造，不是真实故障复现 |
| 视觉一致 | `./scripts/check-project-governance.sh` + `.c-runtime` 子树全量溢出扫描 | 见下 | 面板的 12 格额度轨是量级读法，不是精确刻度；跨 4 个以上工位的真实数据规模未走查 |
| 真实后果 | 8 条 POST 的 method / action / 隐藏字段名 / 错误码与改写前逐字比对 | `local_web.rs` 与 `station_view.rs` 的表单渲染段本轮无 diff，结构未变 | 未实际点击提交任何写动作（会改动真实工位记录） |

**抽屉开合契约（脚本实测，非目测）**

- 静止：`data-open` 无、`inert` 有、`body` 无类、两个触发器的 `aria-expanded="false"`；
- 点击把手：`data-open` 有、`inert` 无、`body.c-rdrawer-open` 有、两者 `aria-expanded="true"`，焦点移到关闭按钮；
- 点击关闭：以上全部复原，焦点回到把手。

**几何实测（1600×900）**

- 把手 112×34 @ (1488, 128)；控制条顶端 168 —— 把手完整落在它上方那条留白里；
- 「去观察目标页」按钮 (1462,185)–(1558,219) 与把手**零交叠**（修前交叠约 70×13px）；
- `.c-runtime` 子树内 `scrollWidth > clientWidth` 的元素：**0 个**。

**窄屏实测（1150×900：正文恰好到达它既有的 1040px 最小宽度）**

- 控制条仍是单行三列 `132px 548px 308px`；仪器面二列；四格读数二乘二；六格读数条六列；
- `.c-runtime` 子树内溢出元素：**0 个**。

**关于窄屏的一段既有行为（本轮不修，留证以免下次误判）**：视口窄于约 1230px 时，页面右侧会被共享外壳裁掉一段。来源是 `.c-page { min-width:calc(var(--c-cell) * 130) }`（=1040px）配 `.v7-app { overflow:hidden }`，两者都是既有规则（`git log -S` 指向 `9188cc8`），不是本轮引入，也不在本轮范围内（ADR-10 冻结共享外壳）。

**本轮修掉的两个缺陷，都出在本轮自己的新代码上**

1. **折叠规则写错了位置**。折叠规则写进了文件早段的 `@media` 块，而它们要覆盖的基础规则排在 300 行之后 —— 媒体查询不改变优先级，只按源码顺序决胜，于是那些规则一条都没生效，1120px 下控制条塌成三行。**定论后没有补写折叠**：实测基础排布在本页最小宽度（992px 内容宽）下零溢出，「塌成三行」完全是失效规则自己造成的。补一套折叠等于为不存在的排版加机制，所以最终处理是把这些规则**整块删除**。
2. **额度轨顶出格子**。`.c-usage-track` 的 `width:calc(var(--c-cell) * 11)` 是定值，装它的格子却随工位卡宽度缩；轨道顶出约 20px，被裁掉的正是读数右边那个数（上限）。加 `max-width:100%` 让轨道跟着格子收 —— 宽屏取值不变，窄屏从裁切改为收缩。

**未做**：未部署、未推送、未合并。`~/Library/Application Support/Linggan Intelligence/runtime-main` 上跑的仍是旧代码，本节描述的效果在本机 `:3000` 上**尚不可见**。

### 5c. 追加修订（2026-09-20 第二轮：Mog 对控制条的四项指示）

**来源**：Mog 当日对着页首那一行的截图给出四项：① 首行不是一个容器块，而是两条线中间容下的内容；② 运行概览重复有两个按钮；③ 这一行不要用新粗野主义做阴影处理；④ 不同状态要进行不同的定义。

**实现**（逐项对应）

1. `.c-deck` 由卡片改为**跟栏**：四边 `border` 改为只留上 / 下两条 `--c-line-rail`；删掉 `box-shadow:var(--v7-brutal)`；底色仍为 `--v7-white`。
2. 删掉控制条里的「运行概览」按钮（首版实现与贴页边的标签重复）。抽屉脚本的 `openers` 用同一个选择器收集两组属性，实测一个入口与两个入口行为一致。
3. 这一行内不落硬影：`.c-deck-actions .c-btn` 的 `box-shadow` 全部取消，悬停位移一并取消（那个 `translate` 本是为投影让位）。作用域限 `.c-deck-actions`，`.c-btn` 在别处照旧。
4. 四档各自定义，见 §4b 新增的三行。

**逐层验收（第二轮）**

| 层级 | 方法 | 实际结果 | 未证明边界 |
|---|---|---|---|
| 视觉一致（形状） | 浏览器读计算样式 | 控制条 `border-top/bottom:2px rgb(17,19,21)`、`border-left/right:0px`、`box-shadow:none`、`background:rgb(255,255,255)`；行内两个按钮 `box-shadow` 均为 `none` | 只有 Chrome |
| 状态定义 | 四态并排预览（真实 `deck_markup` 输出 + 逐字相同的样式表） | 四档各有 `data-state`；色带与字色成对：ok 绿 / partial 橙 / blocked 红 / unknown 中性灰；四档形状一致，只换颜色 | 四态由合成夹具构造，不是真实故障复现 |
| 对比度 | 页内实测四档字色对底色的对比度 | ok 6.90 / partial 3.88 / blocked 7.54 / unknown 4.71（15px 粗体，底为白） | partial 未到 4.5:1，见下「一条既有问题」 |
| 交互 | 脚本实测点开 / 关闭 / Esc | 唯一入口可开可关；`inert`、`body.c-rdrawer-open`、`aria-expanded` 三项同步；焦点移入关闭按钮、关闭后回到标签 | 未用真实指针点击（`element.click()` 验 UI 不可靠，LIDS 既有结论） |
| 回归 | `cargo test -p linggan-api --bin linggan-api` | **240 passed; 0 failed; 21 ignored**，与改前基线逐字相同 | 同上 |

**几何实测（1821×873；数字已减去预览横幅的 31.2px，与 §5b 同坐标系）**

- 控制条 1557×68.4 @ (240, 168)；行内两个按钮纵向 185.2–219.2 —— 与 §5b 记录的顶端 168、按钮 185–219 一致，拆掉左右边框没有改变这一行的纵向几何；
- 标签 112×34 @ (1709, 96.8)–(1821, 130.8)；与控制条顶端相隔 **37.2px** 空白，与行内任一按钮的**纵向交叠均为 0**（横向与「去观察目标页」重叠 72px，但两者不在同一纵向带内）；
- 行内动作：`["新增工位","去观察目标页"]`；全页开抽屉的入口计数：**1**；
- `.c-runtime` 子树内 `scrollWidth > clientWidth` 的元素：**0 个**；`documentElement` 横向滚动：0。

**窄屏实测（1150×873，正文恰好到达既有 1040px 最小宽度）**

- 控制条仍是单行三列 `132 632 228`；六格读数条六列；两个按钮均未折行（高 40 / 34）；
- `.c-runtime` 子树内溢出元素：**0 个**；
- 视口外元素 99 个 —— 全部来自 §5b 已记录的那条既有行为（`.c-page` 的 1040px 最小宽度配 `.v7-app { overflow:hidden }`），本轮未改其分毫。

**本轮定掉的一个做法：色带 + 同色数，底下不铺淡底**

稿里带状态那一格是「3px 色带 + 一层同色淡底往白色退」。照抄后实测四档对比度：ok 6.37 / partial 3.66 / blocked 6.76 / **unknown 4.14**。淡底垫在字下面等于把字和底往中间拉；15px 粗体按正常文字判要 4.5:1，unknown 这一档正是被这条规则拉下水的。因此**整块去掉淡底**，只留色带与同色数 —— 去掉后四档为 6.90 / 3.88 / 7.54 / 4.71，unknown 回到线上。

**本轮的一条既有问题（不在本轮范围内，留证以免下次误判）**：「部分接不了活」那一档在白底上是 3.88:1，没到 4.5:1。它与那条淡底规则无关 —— **这条在 `74e87ad` 里就是这样**（同一档、同一白底、同一个字）。根因在 `--lgi-warning`（字角色）本身的取值，要动它得先动 token 基线并重走一遍对比度验收，不在本轮交付里。

**同一处的一个取舍（由实施者判断，Mog 可改）**：四档里 `unknown` 的色带取 `--v7-unknown` 而非同族的 `-fill`。`-fill` 取自 `--lgi-unknown-dot`，是四色中最浅的，本来就是给列表里一个小圆点用的；拉成 3px 横条压在两行黑线之间几乎看不见，那样「读不到」在这一行里会退化成"没有状态"。

**未做**：未推送、未部署。

### 5d. 追加修订（2026-09-20 第三轮：Mog 对控制条与接单仪器面的七项指示）

**来源**：Mog 当日对着页首那一行与接单仪器面的截图给出七项。

| # | 指示 | 本文对应 |
|---|---|---|
| ① | 运行概览的按钮不在两个线条内 | §5d-1 |
| ② | 新增工位、去观察目标页没有按 design 标准；新增工位几个字没有垂直居中，应跟「登记」按钮一样 | §5d-2 |
| ③ | 截图里那段（行内登记表单）可以删掉，交互改成点「新增工位」弹一个居中弹窗 | §5d-3 |
| ④ | 接不了活这个容器最顶部右侧的纹路没有设计 | §5d-4 |
| ⑤ | 这个英文小标记也需要保留 | §5d-5 |
| ⑥ | 整体的容器高度给我压缩 | §5d-6 |
| ⑦ | 删掉「账号能不能用由服务端逐台判定，插件自己说了不算。」 | §5d-7 |

**决策与偏离（实施前写定）**

**§5d-1 · 恢复控制条内的「运行概览」，整块删除贴边标签。** 第二轮删掉重复入口时留错了另一半：留下的贴边标签虽然滚动可达，却悬在内容区的两条线**之外**，读起来不像这一行的动作。本轮按用户裁定反向操作 —— 入口回到 `.c-deck-actions` 内，`.c-rdrawer-tab` 的标记、三条 CSS 规则与 `body.c-rdrawer-open` 这个类一并清除。**代价如实记录**：按钮随正文滚走，滚到页面下方时够不着。这是当初留标签的唯一理由，由用户在本轮确认接受。

**§5d-2 · 按稿把这一行的按钮分成两级，不是一个模子。** 第二轮删掉的 `.c-deck-actions .c-btn` 覆盖把**三个**按钮一视同仁地压平了，那是过度应用；但本轮第一版又走过了头 —— 把覆盖整块删除，于是三个按钮全部继承了 `.c-btn` 的粗野硬投影。**两个方向都错**。稿里是两级：

| 级别 | 稿中规则 | 本轮落地 |
|---|---|---|
| 主按钮（新增工位） | `.btn-primary` 带 `box-shadow:var(--sp-2) var(--sp-2) 0 var(--border-ink)`；`.btn` 基础类**没有**投影 | 保留 `.c-btn` 的硬投影 |
| 次按钮（运行概览、去观察目标页） | 只有 `.btn` 基础类：白底细边，**无投影** | `box-shadow:none`，只到 `.c-deck-actions` 作用域 |

**「不同状态要进行不同的定义」也照稿逐条落地**（同一批规则，仍只作用于本行）：

| 状态 | 稿中定义 | 本轮落地 |
|---|---|---|
| 次按钮悬停 | `.btn-outline:hover` → 整块反色 `--surface-inverse` / `--text-on-inverse` | 墨底白字；同时 `transform:none; box-shadow:none`，不走本页 `.c-btn:hover` 那套位移＋阴影 |
| 运行概览（抽屉开着） | `body.drawer-open .btn-runtime` → 反色 ＋ `::after` 3px 信号色下划线（`left/right:10px`） | 钩子改用脚本本就维护的 `aria-expanded`，不另挂 `body` 状态类；下划线走 `--c-line-mode`（3px）＋ `--c-signal-mark`，`left/right` 用 `--lgi-space-2`（8px，10px 不是 token） |

选择器必须锚在 `[data-runtime-drawer-toggle]` 上：新增工位同样带 `aria-expanded`（它开弹窗），只写 `[aria-expanded="true"]` 会让它在弹窗打开时也变成黑底 —— 实测已确认该锚定生效。

**垂直居中的根因不是 CSS 值，是元素类型。** 实测文本节点上下留白：「新增工位」`-1 / 22.5`（文字吊在盒子顶边之外）、「去观察目标页」`8.5 / 9`、「登记」`10.5 / 11`。`.c-btn` 不声明 `display` 与 `align-items`，靠原生 `<button>` 的居中；锚点拿不到这份居中。`a.c-btn-quiet` 有单行补丁（`collection_workspace.css:229`），`a.c-btn` 从来没有 —— 全仓库只有这一个元素用 `a.c-btn`。**修法是把「新增工位」改成 `<button type="button">`（弹窗本来也需要它），不动全局 `.c-btn`**，避免为单个用法改动一条贯穿全站的基础规则。改后实测「新增工位」`10.5 / 11`，与「登记」逐位相同。

**§5d-3 · 行内登记表单移入居中弹窗。** 采用本页**已有的模态语言**（`.c-rule-overlay` / `.c-rule-modal-compact`，`collection_workspace.css:577` 起，`collection_control_rule_view.rs:415` 在用）而不是原生 `<dialog>`：本仓库被服务的应用里，模态的既有写法就是这个，`::backdrop` 与 `showModal()` 是这套样式表里没有的新交互面。POST 逐字不变（`method="post" action="/collection/runtime/stations"`、`name="display_name"`、`required`、`maxlength="60"`、`value="本机 Chrome"`、提交按钮、`{failure}` 留在窗内）。**登记失败时弹窗必须自动打开** —— 否则一次失败的登记会渲染成"页面看着没变"，用户不知道发生了什么。**实际做法是控制 `hidden` 属性**（开＝输出 `<div class="c-stn-overlay" data-station-register-overlay>`；关＝多一个 `hidden`），脚本首帧读服务端渲染的那个属性同步一次，不写死成「关」。这一条已由 `a_failed_action_says_so_instead_of_looking_like_it_worked` 双向钉住（有错误必须开着、无错误必须关着），并回填验证过：把判据翻成恒为 `hidden` 后该断言如实变红。

**窗口的可达性契约按本页既有弹窗补全。** 页面原有的 `#c-monitor-rule` 弹窗把 Tab 圈在窗内（`collection_workspace.js:57-83`）。新窗声明了 `aria-modal="true"`，若 Tab 还能走到窗外，声明与实际就对不上，所以同样加了循环（选择器与既有那份逐字相同，不另立一套）。**不用给整页加 `inert`**：那要反过来排除本窗，牵动整页；既有弹窗也没这么做。实测真实按键序列为 `输入框 → 登记 → 关闭 → 输入框 → 登记`，反向 `Shift+Tab` 从「关闭」回绕到「登记」，焦点始终未出窗。

**§5d-4 · 接单仪器面表头加采样栅格。** 用已登记的 `--lgi-mosaic-on-dark`，配**量化遮罩**。四处约束来自文档而不是推断：

| 约束 | 出处 | 本轮取值 |
|---|---|---|
| 只贴右缘、必须配渐隐遮罩、**绝不覆盖文字** | `tokens.md:343` | 右侧独立保留区，四个格子全部落在它左侧 |
| 面积约 **35%–45%** | `tokens.md:343` | **35%**（稿中是 30%，低于登记区间下限） |
| 溶解必须**量化**（硬停止），出现平滑渐变即不合格 | `materials.md:69`、`:113` | 8 段硬停止，比例照稿中 `--quant-r`（8/22/36/50/64/78/90/100） |
| 只能用登记 token，不得写死色值 | 仓库 CLAUDE.md、样式表自带断言 | 用 `color-mix(in srgb, var(--lgi-ink) N%, transparent)`，与 `shell.css:80` 既有量化遮罩同一写法 |

**与稿的一处有意偏离**：稿把 `INSTALLATION_STATE` 压在纹路**上面**，`tokens.md:343` 明写「绝不覆盖文字」。本轮把纹路收进右侧保留区、文字全部让开，构图仍是「深色表头 + 右缘纹路 + 四个格子」，但不与登记规则冲突。若用户要的是稿中那种文字压纹路的效果，需要先改 `tokens.md` 那条规则，不在本轮范围。

> **本条已被第四轮取代（此处保留原文以留痕）**：用户 2026-09-20 明确要求把「3 条通道」与 `INSTALLATION_STATE` 一起移到右侧，即稿中那种文字压纹路的构图。第四轮已去掉该保留轨，改为**文字绘在纹路之上**（`z-index:1`）——`tokens.md:343` 要求的是纹路不得**盖住**文字，而非文字不得落在纹路上方，两者不冲突。详见 §5e-2。

**§5d-5 · 保留 `INSTALLATION_STATE`，并记录 LANG-05 例外。** 这条与 LIDS-LANG-05 冲突，必须显式裁定而不是默默覆盖：LANG-05 只允许三类英文（机器事实 / 系统状态枚举 / 结构编号），并明写「**第四类——描述性标签——禁止**」；`INSTALLATION_STATE` 既不是可枚举状态值、也不是机器标识或编号，正落在被禁的第四类。用户（LANG-05 的决定者）本次明确要求保留，故按**用户授权的单点例外**落地，作用范围仅限本页仪器表头这一格，LANG-05 在别处继续有效。例外同时写入 `../lids/language-policy.md`，避免下一位 Agent把它当违规"修掉"。

**同一处的一条产品语义提示（留证，不改）**：稿中这个标记写的是 `INSTALLATION_STATE`（安装状态），而本页这一块判的是**通道能不能接活**，不是安装状态。照用户指示逐字复刻，语义错配如实记录；若用户希望标记描述本块真实内容，替换成对应英文词即可。

**§5d-6 · 压缩容器高度。** 实测容器 294.8px = 46（表头）+ 244.8（正文），而正文的高度**完全由右栏读数撑起**（`.c-readouts` 202.2 + `.c-readouts-caveat` 42.6），左栏通道列表的自然高度只有 171.8。稿中 `.alert-body` 是 `1.2fr repeat(4,.72fr)` —— **一行四个读数**，这既是压缩杠杆，也是与稿一致的做法。本轮：四格改为一行、读数格内边距降到 `--lgi-space-3`、删掉那句说明（见 §5d-7）。**实测收口**：正文回到 171.8（正是左栏的自然高度），整块 `294.8 → 221.8`，**降幅 24.8%**（比事前估算的 217.8 多 4px，是四格之间 `--c-cell2` 间隙与边框计入后的真实值）。

**§5d-7 · 删除说明句。** 「账号能不能用由服务端逐台判定，插件自己说了不算。」是解释**判定权归属**的散文，不是读数。它随压缩一并删除；`factor_markup` 不再返回该节点，`.c-readouts-caveat` 规则与 `.c-readout-col` 包装层同时清除。

**留证（删除是安全的）**：该句没有独占任何信息。同一块的第 4 格读数本身已写「观察账号 / 每台单独判定 / 见下面工位表的「账号」列」，工位表也有「账号」列与展开里的「接活与账号」面板 —— 删掉的是重复表述，不是唯一来源。

**顺带记一条既有的用词风险（本轮不修）**：`station_view.rs:531` 的注释明确写着「服务端判定的是风险、工位、能力、预算**四样**，账号**不在其中**」，而这句话写成「账号能不能用**由服务端逐台判定**」。两句并非严格互斥（一句说的是「接单判定」的因子清单，一句说的是账号资格由谁持有），但并排出现在同一屏时，读者容易把它读成"账号也参与这次接单判定"。删除后这个歧义随之消失，无需另改。

**实现（逐项对应）**

改动文件：`apps/api/src/local_web/station_view.rs`、`collection_workspace.css`、`collection_workspace.js`。

| # | 落地位置 | 实测证据 |
|---|---|---|
| ① | `deck_markup` 恢复窗内入口；`runtime_drawer_markup` 删除 `.c-rdrawer-tab` 按钮与三条 CSS 规则 | 控制条 `top 199.2 / bottom 267.6`，运行概览 `top 216.4 / bottom 250.4` —— 完全落在两条线之间。全页只剩一个 `[data-runtime-drawer-toggle]`，`.c-rdrawer-tab` 的标记为 0（样式表里那一处是记录本次删除的注释） |
| ② | 「新增工位」改 `<button type="button">`；本轮并修正为**两级按钮**（见上表 §5d-2） | 文本上下留白 `10.5 / 11`，与「登记」逐位相同。实测三级状态：静置——次按钮 `shadow:none`／主按钮 `4px 4px 0`；悬停——次按钮 `bg rgb(17,19,21)` / `fg white` / `transform:none`；抽屉打开——运行概览 `bg rgb(17,19,21)` ＋ `::after` `height:3px` / `bg rgb(239,79,37)`。弹窗打开时新增工位 `aria-expanded="true"` 仍是红底＋硬投影，未被反色规则误伤 |
| ③ | 登记表单移入 `.c-stn-overlay` / `.c-stn-modal`；新增弹窗脚本 | 首帧 `hidden` 为真、`aria-expanded="false"`、`a[href="#station-register"]` 计数 0、表单 `method=post` / `required` / `maxlength=60` / `value="本机 Chrome"` 逐项不变。窗体居中 `910.5` = 视口中心 `910.5`。四条关闭路径（关闭键／Esc／点遮罩／点窗内不关）全部实测通过，焦点去时落输入框、回时落开启按钮 |
| ④ | `.c-instr-head` 加保留轨与 `::after` | 计算值 `conic-gradient` / `background-size:16px 16px` / `opacity:.78` / `pointer-events:none`；遮罩解析为 8 段硬停止（0.15/0.30/0.45/0.62/0.77/0.91/1.0，每档在同一百分比上写两次），无平滑渐变 —— 符合 `materials.md:113`。纹路左缘 `1251.5`，`INSTALLATION_STATE` 右缘 `1241.1`，文字让开 `10.4px` |
| ⑤ | `INSTALLATION_STATE_MARK` 常量 ＋ `.c-instr-en` 一格 | 渲染产物逐字保留；例外登记进 `../lids/language-policy.md`「已登记的单点例外」 |
| ⑥ | 四格改一行、读数内边距降档、删说明句与包装层 | 容器 `294.8 → 221.8`（**−24.8%**）= 表头 46 ＋ 正文 171.8 ＋ 边框 4。正文回到左栏自然高度，四格在 `y=347.6` 同一行、各 `277.9px` 宽（1821 视口） |
| ⑦ | `factor_markup` 不再返回包装层与说明句 | `.c-readouts-caveat` 节点不存在，页面文本不含「插件自己说了不算」与「账号能不能用由服务端逐台判定」 |

**窄屏复核**：1150 与 1024 两档均无横向溢出（`documentElement` 与 `.c-runtime` 溢出计数皆为 0），四格不裁切，纹路与文字仍保持 10.4px 间隙。`.c-page` 的 1040px 下限、以及 900–1040 区间被 `html,body{overflow:hidden}` 裁掉且无滚动条，是**既有壳层行为**：该规则在交付基线 `5371311` 与 `origin/main` 上逐字相同，本轮未触碰，也不在本轮范围内。

**测试与治理**：`cargo test -p linggan-api --bin linggan-api` → 240 passed / 0 failed / 21 ignored，与基线逐位一致（本交付对 `#[test]` 行数增减为 0；`a_failed_action_says_so_instead_of_looking_like_it_worked` 是在既有用例内加断言，未新增用例）。`./scripts/check-project-governance.sh` → passed。样式表无写死色值的硬断言 `collection_stylesheet_authors_no_colour_of_its_own` 通过 —— 遮罩因此用 `color-mix(in srgb, var(--lgi-ink) N%, transparent)` 而非稿中的 `#00000026` 一类字面量。

### 5e. 追加修订（2026-09-20 第四轮：仪器面文字与工位表列对齐）

用户原话：「3 项阻塞 接不了活 3 条通道 INSTALLATION_STATE 这些地方要优化，需要白色字体，3 条通道 INSTALLATION_STATE 这几个要放在右侧，内容区置顶的两条线得向上移动，减小边距，至少缩短目前一半的距离。另外 今日采集 跟 0/200 这个需要居中跟字段名对齐，今日采集字段需要左移一些。」

**本轮的改动面只有一处 CSS 文件**（`collection_workspace.css`），未改 `.rs` / `.js`：四个格子用的都是既有节点，工位表这一列用的是既有类名 `.c-tg-station-grid`（它同时挂在表头行与数据行上，且只出现在这张表），因此**不需要新增任何标记**。

**§5e-1 · 四格文字改白，并连带换掉徽标底色。** 四格统一取深色面自己的白 `--lgi-on-dark`（`lids_tokens.css:15`，`#ffffff`），不再分三档灰。

底色必须跟着换，这一步不是顺手做的：`--lgi-stream-red`（`#e06a55`）是深色面上**色块 / 轨**那一档，配白字只有 **3.28:1**；改前它是深墨字、**5.56:1** 达标——所以「只把字改成白的」会**引入一次对比度回归**。判据在 `ADR-04`：信号色分两个**不可互换**的角色，承载文字的一档与只做色块 / 轨 / 销钉的一档（`tokens.md:340`、`system.md:101`）；`tokens.md:77` 直接把 `signal-fill` 登记为「**承载白字**的填充：主按钮、**实心徽章**」。徽标承载文字，属前一档。

**取 `--lgi-signal-ink` 而非手册里的那一档，理由与代价都写在这里**：运行时镜像 `lids_tokens.css` 里**没有** `--lgi-signal-fill` 这个 token（手册 `tokens.md:77` 登记了值，运行时只有 `signal` / `-hover` / `-ink` 三档），这是**手册与镜像之间的一处缺档**，属治理项，本轮不擅自补。取的是本仓库**已经这么用了两次**的那一档：`comment_study.css:11` 的保存按钮、`comment_research.css:10` 的主按钮，都是 `--lgi-signal-ink` 底配 `--lgi-on-dark` 白字。**实测白字对底 6.71:1**，过 4.5:1。下方 `::before` 那条判定轨仍走 `--lgi-stream-red` 不动——本块原先写的「深色面不复用浅色画布信号色」只约束色块，不约束承载文字的填充。

**§5e-2 · 两个标记移到右侧，纹路构图改为文字压纹路。** 表头由五轨（含给纹路留的空轨）改为四轨 `auto minmax(0,1fr) auto max-content`，内边距改为左右对称的 `var(--lgi-space-4)`。实测四格：徽标 `258–325`、状态 `341–1571.6`、`3 条通道` `1587.6–1633.9`、`INSTALLATION_STATE` `1649.9–1779`；表头右缘 `1795` 减 16px 内边距正是 `1779`，两个标记到此为止。

纹路仍取 **35%**（`tokens.md:343` 登记区间 35%–45% 的下限，实测轨宽 543.5 / 表头 1553）。两个标记落在纹路之上，靠新增的 `.c-instr-head > * { position:relative; z-index:1 }` 保证**文字绘在纹路之上**——这正是稿中 `.instrument-head>*{position:relative;z-index:1}` 的做法。`tokens.md:343` 要求的是纹路**绝不覆盖文字**；文字在上、纹路在下即为满足，与第三轮那版「文字全部让开」是同一要求下的两种构图，不是对规则的修改。`z-index` 只到 1，抽屉是 `--lgi-z-drawer:80`，够不着。

**§5e-3 · 内容区上边距 40 → 20。** `.c-runtime` 原 `padding:calc(var(--c-cell) * 5) 0 …`，首值即 40px，也正是实测的那段距离（内容区顶边 `159.2` → 控制条顶边 `199.2`）。改为 `var(--lgi-space-5)`（20px），**正好是一半**，也是间距梯子上落在「一半」的那一档。实测控制条顶边 `199.2 → 179.2`。

**§5e-4 · 工位表「今日采集」列改为居中。** 改前表头文字右缘 `1169`、数值右缘 `1169`——**两者本来就右对齐**，所以「对不齐」不是边缘没对齐，而是**宽度差**造成的错觉：额度轨宽 `88px`，表头文字只有 `44px`、数值 `33.1px`，右对齐下三个左缘各在一处（`1125` / `1081` / `1135.9`），看上去就是各站一边、表头显得比数字更靠右。改为居中后三者**中线同为 `1123`**：表头 `1101–1145`（左移 `24`，与用户说的「左移一些」对得上）、数值 `1106.4–1139.6`、轨 `1079–1167`。

作用域严格限在这张表：`.c-tg-station-grid` 只出现在工位表的表头行与数据行上，目标表的「作品目录 / 命中作品 / 详情进度」三列仍按 `target_drawer.css:37/40` 的右对齐走——那条规则的理由「位数对齐着比」对纯数字成立，对这一列不成立（它带一条 12 格的轨）。

**留证（本轮不修）**：手册 `tokens.md:77` 登记的 `signal-fill` 在运行时镜像里没有对应 token，而镜像里 `--lgi-signal` 是「单值双用」的 `#ef4f25`——它既不是手册的承字档，也不是手册的色块档。任何在信号色实底上放文字的界面（不止这一处徽标）都会碰到同一个问题。是否给镜像补一档、或把 `--lgi-signal` 对齐手册，是 Token 基线层面的决定，须由用户定夺，本轮只在本页范围内的这一格取已过线且仓库有先例的那一档。

**实现（逐项对应）**

| # | 落地位置 | 实测证据 |
|---|---|---|
| ⓐ | `.c-instr-head` 容器色、`.c-instr-badge`、`.c-instr-badge-bad`、`.c-instr-state`、`.c-instr-code`/`.c-instr-en` 五条规则改 `--lgi-on-dark`；`.c-instr-badge-bad` 底色与边框改 `--lgi-signal-ink` | 四格 `color` 计算值均为 `rgb(255,255,255)`；徽标底 `rgb(167,51,23)`，对白字 **6.71:1**（改前深墨字对 `rgb(224,106,85)` 为 5.56:1，直接换白字则为 3.28:1）；判定轨 `::before` 仍为 `rgb(224,106,85)` |
| ⓑ | `.c-instr-head` 网格改四轨、内边距左右对称；新增 `.c-instr-head > *` 的 `z-index:1` | `3 条通道` `1587.6–1633.9`、`INSTALLATION_STATE` `1649.9–1779`，右缘 `1779` = 表头右缘 `1795` − `--lgi-space-4`；`::after` 宽 `543.5`（35% of 1553）；最右一格 `z-index` 计算值 `1` |
| ⓒ | `.c-runtime` 上内边距 `calc(var(--c-cell) * 5)` → `var(--lgi-space-5)` | 计算值 `padding-top:20px`；控制条顶边 `199.2 → 179.2`（−20，恰为原 40 的一半） |
| ⓓ | 新增 `.c-tg-station-grid .c-tg-head-num, .c-tg-station-grid .c-tg-number-value { text-align:center }` | 表头文字盒 `1101–1145`（中线 `1123`）、数值文字盒 `1106.4–1139.6`（中线 `1123`）、额度轨 `1079–1167`（中线 `1123`）；三格 `text-align` 计算值均为 `center`，表头左移 `24` |

**测试与治理**：`cargo test -p linggan-api --bin linggan-api` → **240 passed / 0 failed / 21 ignored**，与基线逐位一致（本轮未改 `.rs`，`#[test]` 增减为 0）。`./scripts/check-project-governance.sh` → passed。样式表「不得自撰颜色」的硬断言 `collection_stylesheet_authors_no_colour_of_its_own` 通过——本轮新写的注释里一度出现 `#` 字面量（被该断言覆盖的是**整张表含注释**），已改写为 token 名。

**跨表面复核**：`.c-instr*` 只被 `station_view.rs` 使用（`grep -rln c-instr apps/api/src/local_web/*.rs` 仅命中 `station_view.rs` 与其测试），`.c-tg-station-grid` 只出现在工位表的表头行与数据行，两者都不影响其它页面。

### 5f. 追加修订（2026-09-20 第五轮：删一句散文、继续压高度、三个按钮改黑底）

用户原话：「现在没有任何一条通道派得出任务。下面逐条说明卡在哪一样。这个描述文字可以删掉，继续压缩第二个模块的高度。另外 新增工位用黑色填充，然后鼠标移动上去之后变成红色，运行概览跟去观察目标页按钮默认黑色，不带新粗野主义风格的阴影」。

写字之前先问清了一处歧义：「运行概览 / 去观察目标页」的「默认黑色」是黑底还是黑字。用户选定 **黑底实心、无阴影**，悬停与主按钮同族变红。

**§5f-1 · 删掉复述判定的那句散文，并顺势压缩接单仪器面。**

`verdict_markup` 里 `why` 原本是 `&str` 三档。**只删 `Verdict::None` 那一档**，改成 `Option<&str>`、该档取 `None`，模板里的 `<p class="c-instr-note">` 随之条件渲染：

| 档位 | 原文 | 处置 | 理由 |
|---|---|---|---|
| `None` | 「现在没有任何一条通道派得出任务。下面逐条说明卡在哪一样。」 | **删** | 纯复述：判定已经写在表头（徽章「3 项阻塞」+ 状态词「接不了活」）里，下面本来就逐条列着通道。 |
| `Some` | 「还有通道派得出任务，下面这几条现在派不出。它们卡住的原因各不相同，不要当成同一件事。」 | 留 | 不复述判定，说的是**读法**——几种堵塞不能合并成一件事。表头四个字装不下。 |
| `All` | 「下面每条通道现在都派得出任务。这不代表现在有活在跑，只代表申请到这一步不会被挡下。」 | 留 | 同上，「派得出任务」不等于「有活在跑」。 |
| 读不到（`lanes` 为 `None` 的早返回分支） | 「现在读不到「能不能接活」的判定。这不表示系统接不了活，只表示这一页此刻答不出——两者的处置不同。」 | 留 | 这句是仓库不变量要求的分辨（不得从读不到推断出不存在 / 坏了），删掉等于替系统说了一句它没说过的话。 |

**高度实测（同一份数据、同一次渲染，把删掉的散文与 16 的内边距按原样放回去量的「改前」）**：整块 `247.8 → 208`，**−40.0px / −16.1%**。构成：散文本体 `19.8` + 其下外边距 `12` + 内边距 `16 → 12`（上下各 4）= `39.8`。左栏 `197.8 → 158`，右栏四格自然高约 `90`，不是约束项。

**一处如实记录的「改了但当前不生效」**：`.c-instr-issue` 的 `min-height` 同时由 `--c-cell × 4.5` 收到 `× 4`（36 → 32）。今天的数据下**它不起作用**——一行的高度是内容撑出来的（12px 字、18px 行高的备注在 232px 宽的列里折成两行 = 36，加 4+4 内边距与 1px 分隔线 = 45），远高于下限。它只在备注短到一行时才兜底（那时内容 27，被下限从 36 压到 32）。收这一档是为了让上下两处留白走同一条梯子。

**剩下的高度在数据上，不在留白上**：现在这块的高度由每条通道那句备注自己折几行决定。想再矮，只有两条路——把那几句备注写短，或把左栏加宽到备注能排成一行。两条都会动到信息本身，不属于本轮授权范围，留给用户定夺。

**§5f-2 · 新增工位：墨底，悬停变红。**

新增 `.c-deck-actions .c-btn-primary { background:var(--v7-black) }` 与 `:not([disabled]):hover { background:var(--v7-red-hover) }`。硬边投影与悬停位移**保留**（`.c-btn-primary` 原样不动），与页内其它主按钮同款。

**悬停用的红是 `--v7-red-hover`（解析值 `rgb(199,58,21)`），不是 `--v7-red`。** 这一步不是随手挑的：这枚按钮压着白字，按 `ADR-04`，承载文字的信号色需 ≥ `4.5:1`；白字压在 `--v7-red`（`rgb(239,79,37)`）上只有 **3.61:1**——它属于只做色块与轨的那一档；`--v7-red-hover` 实测 **5.19:1**，过线。

**§5f-3 · 运行概览 / 去观察目标页：墨底、无阴影，悬停同样变红。**

`a.c-btn-quiet` 与 `button.c-btn-quiet` 一并覆盖（两条选择器各写一次会漏掉 `<a>`，故按 `.c-deck-actions .c-btn-quiet:not([disabled])` 一条覆盖到底）。**范围只到 `.c-deck-actions`**：本页弹窗里的「登记」、其它页面的 `.c-btn-quiet` 一个字不动。

原先那条「次按钮悬停反色成墨底白字」的规则被本轮取代并删除——默认已经是墨底，反色无处可反。

**抽屉打开时的激活态必须重做**：默认色与激活色原本都是墨底，现在只剩**下缘那道 3px 信号色条**可认。因此该状态下**悬停不再变红**（红条压在红底上会消失，指针一移上去这个状态反而看不见）。规则写成选择器表，靠**源序**压过上面那条通用悬停——两条特异度打平（都是四个类级）。

**实现（逐项对应）**

| # | 落地位置 | 实测证据 |
|---|---|---|
| Ⓐ | `station_view.rs` `verdict_markup`：`why` 改 `Option<&str>`、`None` 档取 `None`、`<p class="c-instr-note">` 条件渲染；`collection_workspace.css` 的 `.c-instr-issues` 内边距与 `.c-instr-issue` 下限各降一档 | 渲染结果中 `c-instr-note` 出现 **0 次**、原句在场 **false**；整块 `247.8 → 208`（−40.0 / −16.1%）；表头 `46`、正文 `158`、边框 `4` |
| Ⓑ | 新增 `.c-deck-actions .c-btn-primary` 与 `:not([disabled]):hover` 两条 | 默认 `background rgb(17,19,21)`、字 `rgb(255,255,255)`、`box-shadow 4px 4px 0 rgb(17,19,21)`、对白字 **18.62:1**；真实指针悬停 `background rgb(199,58,21)`、**5.19:1**、投影放大到 `7px 7px`、位移 `(-2,-2)` |
| Ⓒ | `collection_workspace.css` 控制条那一段整体重写（默认墨底、悬停换红、激活态只留色条） | 两个次按钮默认 `background rgb(17,19,21)`、字 `rgb(255,255,255)`、`box-shadow none`、**18.62:1**；真实指针悬停 `rgb(199,58,21)`、**5.19:1**、投影仍为 `none`、位移 `none`；激活态（真点击打开抽屉）在悬停中仍为 `rgb(17,19,21)`，下缘色条 `3px × 54px`、`rgb(239,79,37)` |

**测试与治理**：`cargo test -p linggan-api --bin linggan-api` → **240 passed / 0 failed / 21 ignored**，与基线逐位一致（删掉的那句散文没有任何测试断言它，`grep` 全仓 `.rs` 只命中源码本身）。`./scripts/check-project-governance.sh` → passed。样式表硬断言 `collection_stylesheet_authors_no_colour_of_its_own` 通过，`#` 字面量计数 **0**（含注释）。

**跨表面复核**：三条按钮规则全部锚在 `.c-deck-actions` 下；`.c-btn-primary` 与 `.c-btn-quiet` 的裸规则未改，故 `station_view.rs:1617` 的「登记」、`:1705` 的「认领」、`:865` 的「更名」等其它按钮外观不变。`.c-instr-note` 的样式规则保留——它仍被「读不到」分支与 `All`/`Some` 两档使用。

### 5g. 追加修订（2026-09-20 第六轮：抽屉左缘去阴影、通道备注截断）

Mog 在同一次走查里给的两条，附一张抽屉左缘的截图。这一轮**只动 `collection_workspace.css` 一个文件**，没有 `.rs` / `.js` 改动。

**Ⓐ 运行概览抽屉左侧：删阴影，留边**

| 项 | 内容 |
|---|---|
| 改前 | `border-left: 2px`（`--c-line-rail`）**加** `box-shadow: -4px 4px 0`（`--v7-black`）。两样叠起来，左缘是一道 **6px** 的黑杠——用户看到的「加粗线条还是阴影」是这一条，不是两样里的某一样 |
| 改后 | 只留 2px 的墨色边，`box-shadow` 整条删除 |
| 为什么边留下 | 这块是**覆盖层**，去掉最后一条边就与底下的内容糊在一起，读不出分界在哪。用户给的是二选一（「线条还是阴影」），删的是阴影那一半 |
| 为什么删阴影够 | 那片阴影的纵向偏移是 `+4px`，而抽屉的底边就压在视口底（`top: 页头高`、`height: 100vh − 页头高`），这 4px 只可能落在视口外——它实际只出现在左缘。删掉不动底缘 |
| 实测 | `box-shadow: none`、`border-left: 2px rgb(17,19,21)` |

稿子里那片柔和的 `rgba` 光晕与本语言所有浮起面本来就不同源，第一轮是拿硬阴影顶的；现在这一处连硬阴影也不用了。

**Ⓑ 通道备注截断，不换行**

用户原话：「寄不了货的提醒：这台工位已有一份有效 Lease，首单观察完成前不会并发领取。这些描述太长了，直接截断，不允许换行，拉高整个行高」——「寄不了货」是「接不了活」的同音误写；末句说的是要消掉的后果（那句备注折行把整个行高顶起来），不是要我把行调高。

| 项 | 内容 |
|---|---|
| 改前 | `.c-lane-name` 让服务端的整句话自由折行。列宽 232px，那句备注折成两行 → 行高 `45 / 45 / 44`（三行还不一样齐） |
| 改后 | `white-space: nowrap` + `overflow: hidden` + `text-overflow: ellipsis`。截断点由列宽决定，**不写死字符数** |
| 行高 | `45 / 45 / 44` → **`32 / 32 / 32`**。32 不是新设的值，是既有的下限 `calc(--c-cell × 4)` 开始生效 |
| 整块 | `208 → 170`（**−38.0 / −18.3%**），表头 46、正文 120、边框 4 |
| 实测 | 三行均 `whiteSpace nowrap`、`textOverflow ellipsis`、`scrollWidth 411 > clientWidth 290`（确实发生了截断）；判定词列 35px、判定码列 79px 宽度不变 |

**随之作废的一条注释**：上一轮在 `.c-instr-issue` 上写过「这个下限在当前数据下不生效，行高是内容撑出来的」。截断之后一行的内容只有 18 的行高加 8 的内边距 = 26，**这个下限开始真正兜住行高**，三条通道因此等高。那句注释已在原位改写，不留两个版本。

**一处信息损失，如实记录**：完整的那句是「这台工位已有一份有效 Lease，首单观察完成前不会并发领取。」，页面上现在止于「…首单…」。原文仍在 DOM 里，但读者在界面上读不到后半句。用户说的是「直接截断」，本轮就按字面执行，**没有自行加 hover 提示**；要不要给截断的那一格补一个 `title` 让人能把整句读出来，作为建议留给 Mog。

**测试与治理**：`cargo test -p linggan-api --bin linggan-api` → **240 passed / 0 failed / 21 ignored**，与基线逐位一致。样式表硬断言 `collection_stylesheet_authors_no_colour_of_its_own` 通过，`#` 字面量计数 **0**。`./scripts/check-project-governance.sh` → passed。

**跨表面复核**：`.c-lane-name` 全仓只有一个使用点（`station_view.rs:543`，主页面仪器面）；抽屉的「规则策略」那一块用的是 `.c-rd-rule-head`，未受影响。`.c-rdrawer` 的阴影是抽屉独有，`.c-rdrawer-tab`（贴边把手）已在第三轮删除。

## 6. 交接

- **修改文件**：`apps/api/src/local_web/station_view.rs`、`collection_workspace.css`、`collection_workspace.js`（+ 本清单、进度记录、LIDS migration log）
- **验证命令/走查**：`cargo test -p linggan-api --bin linggan-api`、`./scripts/check-project-governance.sh`、本机浏览器走查
- **规则或索引同步**：本文件登记进 `docs/design/README.md` 权威地图；`lids/migration-log.md` 补一条本页 v7.2 落地记录
- **例外与替代**：本轮**不替代** `RUNTIME-STATION-TABLE-001` 的任何判定口径；那份清单的判定、四组不可互换区分与语言收敛全部继续有效，本轮只换表达层
- **LIDS migration log / 预览同步**：见上
- **PR / reviewer / integration owner**：待 Mog 指定；按仓库规则，收尾提交前跑一次 `commit-reviewer`，push 与部署由 Mog 决定

### 5h. 最终收口（2026-09-20：单一运行看板、单行工位表与受控材料例外）

Mog 最终确认以单一运行看板收口 `/collection/runtime`：旧控制条不再与看板重复表达；运行概览入口留在看板动作区，不恢复贴边标签；工位表冻结为十列单行，删除详情侧栏。此决定替代本清单中与其冲突的控制条／贴边标签／十一列表达，旧段只作为决策沿革保留。

- **表面与交互**：看板只呈现可接活判定、通道原因、在线／执行／等待／重试／自动观察／并发读数和「新增工位／运行概览／观察目标」三个已有入口。工位更名仍由铅笔进入行内表单，接活仍复用既有 POST；停用、认领窗口、账号确认、能力与上次问活改由同一行的「管理」弹窗承载，不恢复 `<details>` 或侧栏，也不删除既有写动作。
- **数据诚实**：「今天在跑什么」只列 `LiveLease.has_task=true` 的真实任务；只持有 Lease、尚未展开任务的记录不再被写成执行中。顶部读数与本节不互相替代。
- **材料**：Mog 允许本页的量化 mask 与白底纹理，规则精确登记为 `M-02E`。深色表头右缘仍只占 35% 且以八段硬停止遮罩呈现；白底纹理只留在「暂停／恢复」真实动作的右缘，宽度为一个 `--lgi-space-2`。文字、状态色和边框仍承担信息，纹理不承担状态。
- **依赖与非目标**：不改 RuntimeCapacityOverview、StationOverview、RuntimeResourceView、数据库、迁移、调度、准入、额度、账号资格或插件协议；所有 POST 的地址、字段和错误回执保持既有合同。
- **验收补充**：自动测试必须证明一台工位仅有一条 `.c-stn-row`、所有低频动作仍可达、无任务 Lease 不进「今天在跑什么」、有任务 Lease 正确入列；浏览器验收须覆盖管理弹窗开合／Escape 焦点归还、行内更名、暂停／恢复的真实回执、1440px 两台工位的两行密度及 `M-02E` 的两处范围。
- **已完成自动验证**：`cargo test -p linggan-api --bin linggan-api station_view --locked` 42/42、完整 `cargo test -p linggan-api --bin linggan-api --locked` 243 passed / 21 ignored、`cargo check -p linggan-api --locked`、`node --check apps/api/src/local_web/collection_workspace.js`、`verify-ui-design-handbook.sh`、`check-project-governance.sh origin/main` 与 `git diff --check` 均通过。`cargo fmt --check` 仍被未触及的既有文件阻断；本次 `station_view.rs` 已单独格式化，未把全仓基线格式化混入本包。真实 POST、浏览器验收与部署尚待合并后执行。
