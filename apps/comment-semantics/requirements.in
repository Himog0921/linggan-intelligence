# Resolved only for CPython 3.14 on macOS arm64. `requirements.lock` carries every
# transitive pin and hash; the runtime installer accepts binary wheels only.
hdbscan==0.8.44
faiss-cpu==1.15.0
igraph==1.0.0
leidenalg==0.12.0
psutil==7.2.2
