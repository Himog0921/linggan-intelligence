# Fixed by LOCAL-EMBEDDING-001 for the Apple Silicon text-only runtime.
# decord is intentionally absent: its macOS arm64 wheel is unavailable and this profile does not
# accept video requests.
torch==2.8.0
torchvision==0.23.0
transformers==5.2.0
huggingface-hub==1.31.0
sentence-transformers==6.0.1
qwen-vl-utils==0.0.14
accelerate==1.15.0
