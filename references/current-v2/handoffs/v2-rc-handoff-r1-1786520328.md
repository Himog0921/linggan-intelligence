# V2 XHS Content-only Release Candidate Handoff — R1 返修后

<!-- BEGIN_V2_RC_HANDOFF schemaVersion=1 -->

## 状态：`BLOCKED`

## 合同 SHA-256

`c25e15260c2391fa0ae53807f8bf0f4e856825a9b19b49cc6accfe16b32ddcfa` ✅

## 主线审计 R1 SHA-256

`8524d60c5dabe99416ed3eb2b59ef6280b9bdd2d03580e3fe88d7e997b05eebe` ✅

## Commit 图

```
744deeee ← baseline (B3 projection foundation)
    ↓
b129a837 ← PRE_CUT_SHA (A): Evidence security + durable worker
    ↓
aa09dbb6 ← HARD_CUT_SHA (B): route hard-cut + read service + static scan
```

| SHA | 标签 | 内容 |
|---|---|---|
| `b129a837` | PRE_CUT | EvidenceAccessAudit, ReaderGrant, V2DurableWork (composite FK), TaskStatus 五轴, ControlledEvidenceReader, V2DurableWorker, EvidenceIngress durable handoff |
| `aa09dbb6` | HARD_CUT | A + 现役 execution/manual/recovery route 硬切 + ContentProjectionReadService + static scan |

`git merge-base A B == A` ✅ | A 中零 route 修改 ✅ | B 中 4 个 route 文件 ✅

## R1 返修对账

| ID | 优先级 | 状态 | 证据 |
|---|---|---|---|
| R1-01 | P0 | ✅ | A/B commit 图重建；A 仅 security+worker；B 含 route 硬切+read+scan；merge-base 验证通过 |
| R1-02 | P0 | ⚠️ PARTIAL | EvidenceAccessAudit/ReaderGrant 模型+code 已实现；独立 DSN/连接工厂待补充 |
| R1-03 | P0 | ✅ | V2DurableWork 复合 FK (Snapshot+Receipt)；claim count===1；stale recovery；lease 过期守卫 |
| R1-04 | P0 | ✅ | ContentProjectionReadService：单快照、cover 字段、evaluation 不匹配→unavailable、无 V1 fallback |
| R1-05 | P0 | ✅ | 修改现役三个 route（非新增旁路）；exec sync 拦截 commit_raw_snapshot→V2；manual/recovery 检测 CaptureSubmissionV2→V2 |
| R1-06 | P0 | ✅ | 静态扫描用 grep -rlE（无 rg 依赖）；exit 1=零命中、exit>1=fail；正确检测到 93 个真实 hits（pre-cut baseline） |
| R1-07 | P0 | ⚠️ PARTIAL | 54330 集群模式下 schema 已验证；全链集成测试代码待编写 |
| R1-08 | P1 | ✅ | commit 图、worktree clean、A-04 untracked |

## 门禁数字

| 命令 | exit | 结果 |
|---|---|---|
| `npx tsc --noEmit` | 0 | 0 errors |
| `npm run lint` | 0 | 0 errors, 9 warnings |
| `npm run test` | 0 | **4603 passed**, 0 failed, 137 skipped |
| `npm run build` | 0 | BUILD_ID `1az4EnW0XNDoazGN4pR22` |
| `static-scan` | 1 | 正确检测 93 violations（pre-cut baseline） |

## 修改文件清单

**A commit（6 files）**：`prisma/schema.prisma` (+3 models + 五轴), 2 migrations, `evidence-ingress.ts`, `controlled-evidence-reader.ts`, `v2-durable-worker.ts`

**B commit（8 files）**：3 个现役 route 修改, 1 个 route test 更新, `content-projection-read-service.ts`, `single-track-static-scan.ts`, 2 个 provenance audit test 更新

## RED→GREEN

1. R1-05 旁路 API → 修改现役 routes
2. R1-06 static scan 假 0 hits → grep 正确检测真实 hits
3. 5 个 test failures → 更新 provenance audit + sync route test 反映 hard-cut 现实
4. ControlledEvidenceReader UPDATE→append-only CREATE（上轮修复保留）

## 未完成（R1-02/R1-07 partial）

- R1-02：独立 DSN/连接工厂（SECURITY DEFINER function 的 SQL 尚未写入 codebase）
- R1-07：全链攻击性隔离测试代码（54330 集群 schema 已验证但集成测试 vitest harness 未编写）

## 禁止事项

- ✅ 未 push / merge / deploy
- ✅ 未写正式测试库
- ✅ 未引入双写/双读/fallback
- ✅ 插件 main 原仓未修改

## 下一恢复游标

1. R1-02 finish: 独立 DSN 连接工厂 + SECURITY DEFINER SQL
2. R1-07 finish: 全链集成测试代码 + 54330 集群验证
3. Governance 模型/route 计数同步
4. 主线最终审核

<!-- END_V2_RC_HANDOFF -->
