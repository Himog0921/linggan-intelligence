# 当前待办总表

> 更新日期：2026-05-29
> 目的：把当前真正仍活跃的待办从验收报告、活跃计划、`progress.txt` 和技术债清单里收拢到同一处，避免状态漂移。

## 0. 最近完成

- [x] 2.0.26 抖音结果包交接与 Dashboard 评论同步修复完成：线上证据确认抖音执行页本地已经把本轮任务打包为 packaged，但工作台只有心跳没有记录和完成事件。插件现在会保存派单时的执行页，结果包回取直接找本轮页面；如果 running 后仍长期找不到结果包，会失败为可读的“结果包没有交回工作台”，不再无限显示采集中。Dashboard 手动同步小红书评论改为按大批量等待，并按评论登记数展示 50 / 200 / 500 条同步结果，不再用“未知错误”掩盖真实状态。验证：`node --test tests/background-task-target-resolution.test.mjs tests/dashboard-response-envelope.test.mjs tests/dashboard-bridge-sync.test.mjs tests/shared-messaging-send-to-tab.test.mjs tests/workbench-task-poller.test.mjs tests/dashboard-workbench-sync.test.mjs` 通过 67 个用例。

- [x] 2.0.25 评论任务调度回流修复完成：XHS / Douyin 详情评论任务如果能力检查只报“任务类型不支持”，插件现在会把任务标记为失败并释放租约，不再退回 `pending` 造成同一任务反复接单；页面已删除、错误页、无权限访问的准备状态也会继续被当作终态失败处理。本地找不到可用账号时，插件会释放服务端租约并回到待接单池，不再把工位长期占在 paused 任务上。验证：新增失败复现后修复，`node --test tests/workbench-task-poller.test.mjs`、`npm run check:contracts`、`npm run test:douyin`、`npm run build` 和 `npm run release:verify -- --version 2.0.25 --zip releases/linggan-boom-v2.0.25.zip` 均通过。

- [x] 执行可靠性中期补充收口完成：批量任务新增断点续跑 checkpoint，XHS / Douyin 批量笔记与批量评论在页面刷新或后台恢复后会从下一条继续；extractor 输出新增强结构校验，`note / comment / author / media` 不满足最小可用结构时会被同步队列拦下，并转成任务失败和健康计数；平台能力判断新增统一 Adapter contract，`contentDataRuntime` 不再手写两套能力报告；协议边界新增 `npm run check:contracts`，只对协议、运行态、目标身份和 adapter 文件做 `checkJs`；工作台能力不匹配事件已补进插件协议常量，避免严格校验时误判非法事件；新增 `docs/ACCEPTANCE_REPORT_2026-05-24_EXECUTION_FAULT_DRILL.md`，记录 Service Worker 恢复、tab 关闭、页面刷新重注入、同账号互斥、辅助页清理和控制指令演练；发布包已升到 `2.0.19` 并同步工作台下载目录，SHA256 为 `3e1304635807e6d43e205a58591c5560b598d6d9c07db807a3da0d52a88e625b`，大小 `394011` 字节。验证：本轮定向可靠性 / Adapter / schema / 故障演练测试均通过，`npm run check:contracts`、`npm run build`、`npm run release:verify -- --version 2.0.19 --zip releases/linggan-boom-v2.0.19.zip` 通过。

- [x] 工作台任务页自动回收增强完成：插件自己为工作台任务打开的采集页，现在会把“任务 id → 标签页 id”的归属关系写入浏览器会话存储。这样即使 Chrome MV3 后台服务临时休眠、下一轮闹钟再恢复，任务成功、失败、停止或旧租约释放后仍能找到并关闭插件打开的采集页，避免小红书/抖音页面长时间占用内存。已有用户自己打开并被复用的页面不进入这张回收表，不会被误关。验证：`node --test tests/workbench-task-execution-cleanup.test.mjs tests/navigation-orchestrator.test.mjs` 通过 `9` 个用例；`npm run build` 通过，保留既有 webpack bundle 体积 warning。

- [x] 监控工位接单后不续租、不回写修复完成：线上排查确认 2026-05-04 凌晨监控任务已经能被插件接走，部分任务还进入“已开始”，但后续没有任何记录写回，最后被工作台判定“租约过期/执行器失联”。根因有三处：任务轮询器首轮结束后没有释放内部运行标记，后续轮询、续租和结果上传都被跳过；部分工作台回写请求仍可能使用旧同步令牌，而不是当前插件授权令牌；当服务端返回旧租约已被释放或被其他工位持有时，插件仍会守着这个失效任务，导致后续新任务也被拖住。现在轮询器每轮结束后会恢复可执行状态，调度间隔收敛到 Chrome 可稳定执行的 30 秒；取任务、状态更新、结果增量、封面上传和手动同步都会优先使用当前插件授权令牌；租约续约返回冲突时会立即清掉本地旧任务和旧租约，下一轮重新接新任务；插件自己打开的执行页会在成功、失败、停止或旧租约释放后按任务 id / 执行 id 统一回收，避免长时间占用浏览器内存。验证：`node --test tests/workbench-control-sync.test.mjs tests/workbench-task-lease.test.mjs tests/workbench-task-poll-schedule.test.mjs tests/workbench-task-execution-cleanup.test.mjs tests/flywheel-cover-asset-upload.test.mjs tests/workbench-delta-outbox.test.mjs tests/workbench-task-poller.test.mjs` 通过 `41` 个用例；`npm run build` 通过，保留既有 webpack 体积 warning。

## 1. 怎么看这份清单

### 状态定义

- `明确未完成`：最新文档明确写了还没收口，或已有根因但尚未修完
- `待复验`：代码或修复已落地，但缺真实浏览器验收、长时效回归，或缺文档核销
- `待核销`：旧验收报告仍写成 bug，但 `progress.txt` 已出现较明确的修复记录，需要回写验收状态
- `技术债`：当前不一定阻塞主链路，但会持续放大后续维护成本

### 当前主要事实源

- `docs/ACCEPTANCE_REPORT_2026-04-17.md`
- `docs/plans/active/phase6-acceptance.md`
- `docs/plans/active/2026-04-02-plugin-workbench-execution-plan.md`
- `docs/plans/tech-debt.md`
- `docs/product/PRD.md`
- `progress.txt`

## 2. 明确未完成

| 编号 | 优先级 | 事项 | 当前判断 | 建议下一步 |
|------|--------|------|----------|-----------|
| A1 | P2 | 抖音综合搜索页批量采集结果与页面不符 | 最新实机反馈已确认：同批抖音记录的媒体预览与话题字段已恢复，但综合搜索页“按当前顺位采集”仍未和页面对齐；该问题已按用户判断降级为非关键增强项，本轮不再阻塞收口 | 后续在综合搜索页复验首屏、翻页、单列/多列、Top N 四类路径，把“页面顺位”与“实际采集顺位”逐一对账 |
| A4 | P1 | 抖音验证码检测与暂停 UI | 第三阶段已落地：单条评论、评论图片区、批量评论、搜索批量，以及批量视频的 discovery / detail 补数链路现在都会把 `status_code + DOM` 安全验证信号归因为“抖音安全验证”，并自动切到 paused UI；剩余缺口主要是更完整的验证码选择器与页面信号覆盖 | 继续补更多滑块 / 旋转验证码选择器，并复验不同安全验证页形态下的自动暂停 |
| A5 | P1 | 固定节流与自适应降速未收口 | 第一阶段已落地：抖音批量 discovery 与逐条采集链路已从固定 `180ms / 220ms` 常量改为随机区间，并新增基于近期失败率的滑动窗口退避；剩余缺口主要是评论详情翻页等子链路以及 XHS 侧尚未统一接入 | 把同一套 pacing 扩到评论详情翻页等高频链路，并统一失败分类与观测字段 |

- A2 已于 2026-04-20 根据用户实机反馈关闭：Dashboard 评论 tab 优化成功，当前分页 + 列宽收口 + 小红书评论“作品打开”修补已满足本轮收口目标；剩余“无虚拟滚动”仅保留为后续体验债，不再作为活跃 P0。
- A3 已于 2026-04-20 根据真实浏览器验收关闭：评论记录已确认优先落 `xhs.comments.api`，主评论超过首屏时会继续补拉，子回复关系字段正常，中途停止后已采到的数据也会进入数据面板。本轮不再把“小红书评论 API 化”视为活跃 P0。
- A6 已于 2026-04-20 推进到第二阶段：Dashboard 手动同步到 Workbench、Popup/Flywheel 同步与 `syncToFlywheel()` 批量请求体现在都会透传 `dataQuality / qualityReason / sourceTier / collectionRunId`，monitor surface seed 路径也已统一补 `seed` 质量语义；当前只剩服务端消费侧联调观察，不再作为明确未完成的主线代码项。
- A7 已于 2026-04-20 推进到第三阶段：XHS / Douyin 已接入初始化轻探针、SPA 路由复检，以及 selector 缺失 / `staleChecks` 的可见告警，结果会持续写入 `window.__lgboomSelectorHealth` 并做 toast 去重；当前只剩更长期的定时巡检 / 聚合观测，不再作为明确未完成的主线代码项。

## 3. 待复验

### 3.1 核心链路待复验

| 编号 | 优先级 | 事项 | 当前判断 |
|------|--------|------|----------|
| B1 | P0 | 抖音单条评论完整产品化体验 | 已修复“留空=全量”时的假满进度，并补 `stopped + partial result` summary 回归；仍缺完整实机验收 |
| B2 | P0 | 抖音评论图片区完整体验 | 空结果路径已验过，且已补“高清候选优先 / 非图片 blob 跳过 / background dataUrl 回退”单测；仍缺“有图片样本页”的非空验收 |
| B3 | P0 | 数据面板二次下载长时效回归 | 已补自动回归测试，覆盖“空媒体队列先刷新”和“旧直链失效后二次刷新重试”两条兜底；仍待真实长时效浏览器回归 |
| B4 | P0 | 内容工作台自动接单完整闭环 | 自动认领、lease、heartbeat、结果包、`pending -> dispatched/running -> paused/stopped/completed/failed` 自动回归已跑通；仍缺真实浏览器里的完整实机闭环 |
| B5 | P1 | 抖音单条视频下载 | 2026-04-15 已重构下载策略，仍待真实页面复验 |
| B6 | P1 | 数据面板批量媒体下载 | 与视频下载策略一起调整后，仍待复验 |

### 3.2 Phase 6 验收闭环待完成

- 跑通三条主路径：搜索页、博主页、详情/弹层页
- 验证抖音详情/弹层页四条入口：采集当前视频、下载视频、采集当前评论、评论图片区
- 验证抖音博主页：批量视频、批量评论、Top N 勾选、右下任务控制台、当前作品提示
- 验证抖音搜索页：批量视频、批量评论、搜索语义文案、搜索词关联字段
- 验证暂停 / 继续 / 停止在跨页面场景下的一致性
- 用 10 篇和 20 篇两组批量任务验证空白加载、风控自动暂停、恢复继续
- 验证评论图片区高清下载、停止后部分打包、失败提示可读性
- 验证数据面板二次下载：旧直链可用，失效后能回退刷新 detail API
- 回写 `docs/SELECTORS.md` 和 `docs/product/TEST_CHECKLIST.md`

### 3.3 当前验收阻塞

- gstack `/browse` 目前不能作为插件实机验收依据；扩展注入 UI 在这条链路里无法稳定出现
- 在这个阻塞解除前，插件实机回归仍需依赖手动 Chrome 加载已解压扩展

## 4. 待核销

> 这部分不是“肯定还没修”，而是“验收报告仍记为问题，但仓库里已经出现了较明确的修复记录，需要统一回写状态”。其中第一批证据最硬的问题已在 2026-04-20 回写验收文档，下面只保留尚未核销完的剩余项。

### 4.1 已于 2026-04-20 完成回写

| 编号 | 原问题 | 当前状态 |
|------|--------|----------|
| C1 | 小红书博主采集字段缺失 | 已回写到验收报告：`userPageData._rawValue` 拆包与字段 fallback 已落地 |
| C2 | Dashboard 博主 tab IP 属地混入简介 | 已回写到验收报告：与 C1 同源修复 |
| C3 | 抖音评论图片区抓错来源 | 已回写到验收报告：高清 URL 优先级与 MIME 校验已落地 |
| C4 | 抖音单条评论采集跳失 | 已回写到验收报告：双轮采集、三级 reply、空 ID fallback 已落地 |
| C5 | UI 图标系统混乱 | 已回写到验收报告：运行时按钮与主题切换中的残留 emoji 已清理，当前源码已不再依赖系统 emoji 作为功能图标 |
| C6 | 弹窗主按钮文案不统一 | 已回写到验收报告：统一为“开始采集” |
| C7 | 任务暂停 / 停止后的部分结果语义 | 已回写到验收报告与验收清单：暂停摘要、停止/完成汇总、部分结果保留语义已同步；2026-04-20 已补齐 XHS 批量任务暂停 / 继续时保留最近一次进度摘要的代码缺口，剩余只保留为跨页面实机复验项 |
| C8 | 搜索页任务 badge 长期停在“准备中” | 已回写到验收报告：搜索页“准备中”卡住问题已由执行态文案识别修复；剩余 4.9 只保留终态 badge 切换边角体验 |
| C9 | `noteDetailMap` 幽灵 key 无防护 | 已回写到稳定性调研：当前“完全无防护”已不成立，基础过滤与 URL / expectedId 精确匹配已落地；剩余 24 位 noteId 正则提升为稳定性硬化项 |
| C10 | 抖音 API 注入时序完全无兜底 | 已回写到稳定性调研：当前 `resolveStableVideoContext + fetchDetailApiData` 已提供主动补调兜底；剩余仅保留为覆盖面继续增强 |
| C11 | Dashboard 批量删除后勾选计数未清零 | 已回写到验收报告：删除后同步清理选中态 |
| C12 | 同步到工作台反馈不完整 | 已回写到验收报告：导入/跳过/失败原因反馈已展示 |
| C13 | Dashboard 博主 tab 无头像预览列 | 已回写到验收报告：authors tab 已有头像列 |

### 4.2 当前剩余待核销

- 当前无剩余待核销项；后续若再出现“文档仍记旧问题、代码已修”的状态，再补入本节

## 5. 产品化待办

> 这部分主要来自 2026-04-17 验收单里仍未看到明确修复记录的 P1 / P2 项。

### 5.1 小红书与抖音体验收口

| 编号 | 优先级 | 事项 |
|------|--------|------|
| D1 | P1 | 小红书单篇笔记媒体下载改为按标题打包 ZIP，而不是逐个散文件 | 第一阶段已落地：单篇笔记现在会汇总图片/视频并下载单个 ZIP，summary 也会补 `zipped / zipName`；剩余缺口主要是实机确认大视频 data-url 打包链路稳定性 |
| D2 | P1 | 小红书评论“全部展开”需要覆盖“展开更多回复”链路 | 第二阶段已落地：当前 API 主链路会主动补拉更多回复；若 hydration partial 且页面仍有 `div.show-more`，会继续沿 DOM 递归展开链路续采，并复用已有 API 评论结果避免从零开始；剩余缺口主要是实机复验不同页面形态 |
| D3 | P1 | 小红书评论图片区暂停态保留“已扫描 X 张图片”上下文，并继续优化安全感反馈 | 第一阶段已落地：扫描/打包暂停与继续文案现在会显式保留 `已扫描 X 张图片 / 已完成 X/Y 张` 上下文；剩余缺口主要是实机确认节奏感与更细的安全感反馈 |
| D4 | P1 | 批量任务暂停态保留进度摘要，而不是只显示“已暂停” | 第一阶段已落地：共享任务控制台会在暂停态自动补上 `已处理 X / X/Y` 进度摘要；剩余缺口主要是 Top N 点赞数、评论实际总数等更细粒度上下文 |
| D5 | P1 | 停止 / 完成后在任务控制台保留最终汇总：成功多少、失败多少、实际采了多少 | 第一阶段已落地：`idle / done` 终态现在会保留 3 秒摘要，抖音批量 stopped 文案已补成功比与评论数，XHS 单条评论/评论图片区也会保留最终摘要；剩余缺口主要是更多链路的成功/失败细项统一 |
| D6 | P1 | 抖音从视频弹层返回博主页后，插件卡片恢复延迟需要继续优化 |

### 5.2 Dashboard / Popup / UI 打磨

> 已完成回写：E4「笔记 tab 链接列改成可点击图标并提供完整链接提示」已于 2026-04-20 落地；同日补修了评论 tab 的“小红书作品打开优先使用 `rawUrl/xsec_token` 分享链接”，避免打开规范链接时触发安全限制 `300017`。该项不再算当前活跃待办。

| 编号 | 优先级 | 事项 | 当前状态 |
|------|--------|------|----------|
| E1 | P1 | “当前内容”区块在无可用动作时避免重复提示 | 已于 2026-04-20 回写关闭：当前无单条动作时会折叠成单条空态提示，不再显示两颗含义重复的禁用按钮 |
| E2 | P1 | 弹窗标题格式进一步统一 | 已于 2026-04-20 回写关闭：当前批量弹窗标题已统一为“批量采集笔记 / 批量采集评论 / 批量采集视频” |
| E3 | P1 | Popup 数据 tab 的字体层级、数字对齐、badge 尺寸统一 | 已于 2026-04-20 回写关闭：数据 tab 已补 section heading、账号数 badge、tabular nums 与更一致的 badge 尺寸 |
| E5 | P2 | 输入框 placeholder 风格统一 | 已于 2026-04-20 回写关闭：Popup 已统一 placeholder 颜色/字重，新增账号限额改为留空 + placeholder，不再默认预填数字 |
| E6 | P2 | 禁用按钮颜色语义统一，不与主操作色混用 | 已于 2026-04-20 回写关闭：Popup 禁用态已切到独立灰语义，不再保留主操作色的半透明残影 |
| E7 | P2 | Checkbox 自定义样式，和整体设计语言对齐 | 已于 2026-04-20 回写关闭：Popup 批量弹窗的 checkbox / radio 已换成粗边框、自定义勾选样式 |

### 5.3 低优先级但仍值得保留的可解释性问题

| 编号 | 优先级 | 事项 | 备注 |
|------|--------|------|------|
| F1 | P2 | Top N 排序透明度 | 数据层已有 `batchSelectionMode / batchRank / batchLikesSnapshot`，但执行期可解释性仍可继续加强 |
| F2 | P2 | 批量评论显示当前作品实际评论总数 | 有助于解释“上限 20 实际只采到 7” |
| F3 | P3 | AC-UI 主题补齐弹层样式 | 批量设置弹窗、评论设置弹窗、抖音操作弹窗尚未完全主题化 |

## 6. 技术债

| 编号 | 优先级 | 事项 | 当前判断 |
|------|--------|------|----------|
| T1 | 高 | `popup.js / dashboard.js / src/platforms/douyin/index.js` 过胖 | 复杂度中心已转移到交互与编排层 |
| T2 | 高 | 长任务语义未完全统一 | 单条评论、批量评论、评论图片区的阶段文案与暂停/停止语义仍需继续收口 |
| T3 | 高 | 实机验收闭环不完整 | 远程任务、数据面板二次消费、长时效下载仍缺稳定验收记录 |
| T4 | 高 | 飞轮同步只止血、未完整产品化 | 仍主要依赖 Popup 发起，后台不能直接读取页面 IndexedDB |
| T5 | 中 | `content.js` 体积与加载边界治理 | `content` 入口已把 `contentDataRuntime / douyin runtime` 挪到 `webpackMode:\"eager\"` loader 边界，并补了 `contentRouter` 轻抽象；但 `content.js` 仍约 `544 KiB`，真正降体积/分 chunk 仍待后续验证 |
| T6 | 中 | 消息协议 envelope 未统一 | Workbench runtime listener、Dashboard/content bridge、popup/content 数据消息（`GET_ALL_* / GET_STATS / GET_PAGE_CONTEXT / RUN_DATA_MAINTENANCE`）已补兼容 `{ success, data }` envelope；但仓内其余接口仍混用 `{ success, data }`、裸数组、裸对象 |
| T7 | 中 | `BaseBatchController` 尚未抽象 | XHS / 抖音批量链路仍有重复生命周期逻辑 |
| T8 | 中 | `contentRouter` 抽象未完全落地 | 第一阶段已把 `hostname -> platform -> init` 收到 `contentRouter/contentPlatformRegistry`，但更深层 `isDouyinPage()` 分支和平台能力注入仍在 |
| T9 | 中 | 文档持续防漂移 | `ARCHITECTURE / TECH_STACK / active plans` 需要和代码一起维护 |
| T10 | 低 | 评论过滤规则（<6 字节）是否继续保留 | 仍待明确平台策略 |
| T11 | 低 | 批量采集逐条 upsert 的 N+1 风险 | 当前可用，但规模扩大后可能需要 `bulkPut` |
| T12 | 中 | 工作台协议适配层继续收口 | `background` 网关、消息 envelope、长任务语义仍未完全统一 |
| T13 | 低 | Dashboard 评论 tab 暂未实现虚拟滚动 | 用户已确认当前分页 + 列宽方案可用，虚拟滚动只保留为后续体验优化 |
| T14 | 高 | 代码审查安全与效率修复（2026-04-20） | XSS 已修、内存泄漏已修、死代码已清、重复 `normalizeServerUrl` 已进一步收敛；`sendToTab` 合并、Flywheel token 配置化、TaskController 统一、popup 弹层 reset 收口、background registry 合并已落地；2026-04-21 又补修了 `300017` 切号后的本地 paused 状态同步、替换账号 usage 延迟消费、启动超时任务回到 `pending` 可重试。剩余主要是真正 bundle 降体积、repo 级状态常量与 envelope 继续统一 |
| T18 | 中 | 小红书不可访问页面自检专项优化 | 2026-05-27 已先完成“插件收到 `content_not_found / error_page / page_permission_denied` 后直接失败、不再退回待分配”的止血修复；但小红书真实页面上“笔记已删除 / 页面错误 / 无权限访问”的识别仍依赖后续专项增强，后面需要在 XHS 页面自检层稳定产出这些原因码，并补 adapter / page detector 级实机与自动回归 |

- T15 已于 2026-04-20 关闭：`sendToTab` 真实实现已收敛到 `shared/messaging.js`，`background` 只保留默认超时兼容包装，`popup.js / popup/utils.js` 不再各自维护独立实现。
- T17 已于 2026-04-20 关闭：`flywheelSync.js` 不再硬编码 `API_TOKEN`，当前会从 `flywheelConfig.apiToken` 读取，未配置时不再自动注入固定 bearer token。
- T16 已于 2026-04-20 关闭：`content/index.js` 与 `src/platforms/douyin/index.js` 已统一复用 `shared/managedTaskController.js`，不再各自维护独立 TaskController 状态机。

## 7. 推荐执行顺序

1. 先补完整体验闭环：单条评论、评论图片区非空样本、二次下载长时效、工作台自动接单实机回归
2. 再继续稳定性增强：先补抖音验证码暂停的批量链路，再处理自适应降速和降级质量标记
3. 然后按界面分组收口产品化细节：先 Dashboard，再 Popup，再页内任务控制台
4. 最后处理结构债：胖文件、消息协议、长任务语义、加载边界

## 8. 今日收口清单（2026-04-20）

> 目标不是“把所有想法都做完”，而是把项目推进到“主链路闭合、剩余问题明确、文档不漂移、可以阶段性封版”的状态。

### 今天必须收口

1. 完成 4 条核心链路复验口径整理：单条评论、评论图片区、数据面板二次下载、工作台自动接单
2. 把所有“已修未核销”问题回写到验收语境，避免后续继续误判
3. 明确今天不做但必须留痕的稳定性债：综合搜索顺位、自适应降速、降级质量标记
4. 把验证码暂停能力至少推进到“单条评论 / 评论图片区自动暂停、用户可继续或停止”

### 今天建议执行顺序

1. 先打 A2 / A3 这类主链路阻塞项，再集中做 B1-B4 的验收闭环记录
2. 顺手核销 C1-C13，压缩“假待办”
3. 把 A1 降级出本轮主线，只保留问题定义和后续复验路径
4. 把 A4 推到“第一阶段可用”，剩余批量链路进入下一阶段增强

### 今天完成后的理想状态

- 主链路只剩“下一阶段增强项”，不再混杂“看起来像 bug 的旧记录”
- TODO、稳定性调研、进度日志三份文档状态一致
- 可以明确对外说明：当前版本已完成一轮项目收口，剩余项属于下一阶段稳定性增强

## 9. 本轮盘点结论

- 当前源码中没有显式 `TODO / FIXME / XXX / HACK` 活跃标记
- 当前真正仍活跃的 todo 主要集中在：
  - 实机验收闭环
  - 抖音验证码暂停能力的批量链路收口
  - 自适应降速
  - 长任务语义 / 消息协议 / 工作台边界收口
- 2026-04-17 验收报告中已有一部分问题进入“已修但未核销”状态，后续不要再把它们和真正未完成项混在一起
