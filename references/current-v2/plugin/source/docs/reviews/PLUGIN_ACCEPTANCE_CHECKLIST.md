# 灵感爆爆爆插件验收清单

> 验收目标：判断插件能不能稳定承担内容工作台采集执行端  
> 当前状态：清单已输出，Step 3 已完成可自动化只读验收；P0-1 回写队列、P0-2 Dashboard 消息桥均已完成代码层复核；真实采集和远程工位联调仍待执行  
> 前置要求：插件已加载 `dist/`，内容工作台可访问，插件已授权并完成工位配对

## 1. 基线验收

| 动作 | 通过标准 |
|---|---|
| 运行 `node --test tests/*.test.mjs` | 288 个测试全部通过 |
| 运行 `npm run build` | 构建成功，只有体积警告，没有错误 |
| 运行 `npm audit --omit=dev` | 生产依赖 0 漏洞 |
| 加载 `dist/` 插件 | Chrome 扩展页无 manifest 错误 |

## 2. 小红书采集验收

| 场景 | 操作 | 通过标准 |
|---|---|---|
| 单篇笔记 | 打开小红书笔记详情，点击采集 | 标题、正文、作者、赞藏评、封面、链接完整 |
| 评论采集 | 在笔记详情采集评论 | 一级评论和子评论按设置数量返回 |
| 作者页采集 | 打开博主页，采集作者信息和作品 | 作者资料完整，作品数量接近目标数 |
| 搜索页批量 | 搜索关键词后批量采集 30-50 条 | 不重复，不明显漏字段 |
| 媒体账本登记 | 同步一条有封面和视频的作品到工作台 | 工作台确认媒体已登记并进入标准处理队列；插件没有单独上传封面，页面最终只从工作台媒体账本交付稳定媒体 |

## 3. 抖音采集验收

| 场景 | 操作 | 通过标准 |
|---|---|---|
| 单条视频 | 打开抖音视频详情，点击采集 | 标题、作者、互动数、封面、视频链接完整 |
| 评论采集 | 在视频详情采集评论 | 评论数量、作者、内容、点赞数完整 |
| 评论图片 | 采集含图片的评论 | 图片能下载或回写失败原因 |
| 作者页批量 | 打开抖音作者页批量采集 | 作品列表能展开到目标数量 |
| 搜索页批量 | 搜索关键词后批量采集 | 结果可稳定滚动并生成记录 |

## 4. 远程工位验收

| 场景 | 操作 | 通过标准 |
|---|---|---|
| 工位注册 | 输入配对码注册插件工位 | 内容工作台显示该工位在线 |
| 心跳 | 保持插件打开 3 分钟 | 工位在线状态持续刷新 |
| 接单 | 工作台下发一条小红书任务 | 插件领取任务并打开/复用正确页面 |
| 续租 | 任务运行超过 5 分钟 | 任务不会因为租约过期中断 |
| 停止 | 工作台点击停止任务 | 插件停止采集并回写 stopped/stopping 状态 |
| 页面未启动 | 关闭目标页后派发任务 | 45 秒后任务释放，后续可重试 |

## 5. 数据回写验收

| 场景 | 操作 | 通过标准 |
|---|---|---|
| 正常回写 | 完成一次远程任务 | 工作台出现任务记录、执行日志和采集结果 |
| 断网重试 | 采集中断网 1 分钟再恢复 | 本地队列自动补发，工作台数据不丢 |
| 插件重启 | 回写中关闭浏览器再打开 | 未确认的数据会在约 5 分钟超时后自动重试 |
| 重复回写 | 同一批数据重复发送 | 工作台不重复入库 |
| 归属校验 | 换一个使用者账号同步 | 数据写入当前绑定账号/工作区，不串号 |

## 6. 用户可见流程验收

| 场景 | 操作 | 通过标准 |
|---|---|---|
| Popup 首页 | 打开插件弹窗 | 能识别当前平台和页面类型 |
| 账号管理 | 添加/删除采集账号 | 成功提示清楚，失败提示可理解 |
| Dashboard | 打开本地数据面板 | 能查看、删除、同步本地采集数据 |
| 页内按钮 | 在小红书/抖音页面看按钮 | 按钮不遮挡页面主要内容，点击有反馈 |
| 错误提示 | 未登录、风控、无权限场景 | 用户能看懂下一步要做什么 |

## 7. 阻塞判定

出现以下任一情况，不建议通过企业级验收：

1. 远程任务完成但工作台缺少采集结果。
2. 断网或重启后，本地队列不再自动补发。
3. 页面脚本可伪造 dashboard 消息触发同步或删除。
4. 抖音远程任务无法说明使用哪个账号执行。
5. 插件把资源不可用全部记成真实采集失败。

## 8. 下一轮实机验收执行步骤

这部分需要在真实浏览器、真实账号和真实工位环境里跑。执行时只需要记录“通过/不通过 + 现象截图或一句话描述”，不需要判断代码原因。

### 8.1 P0-1 断网/重启回写验收

| 步骤 | 操作 | 通过标准 |
|---|---|---|
| 1 | 打开内容工作台，确认插件已授权、工位在线 | 工作台能看到该插件工位在线 |
| 2 | 打开一个小红书笔记或博主页，触发一次小规模采集或远程任务 | 插件开始执行，页面有进度反馈 |
| 3 | 采集中途断网 1 分钟，再恢复网络 | 插件不需要人工重新采集，恢复后会继续补发 |
| 4 | 到工作台查看对应任务或同步结果 | 最终能看到采集数据、执行日志和结果记录 |
| 5 | 再做一次“回写中关闭浏览器，5 分钟后重新打开” | 未确认数据会自动重试，不长期卡住 |

### 8.2 P0-2 Dashboard 伪造消息拦截验收

| 步骤 | 操作 | 通过标准 |
|---|---|---|
| 1 | 打开插件 Dashboard，本地数据面板正常展示 | Dashboard 可正常读取数据 |
| 2 | 在平台页面控制台发送一条不带 nonce 的伪造 dashboard 消息 | 插件拒绝处理，不触发删除、同步或读取动作 |
| 3 | 再发送一条带错误 nonce 的伪造消息 | 插件仍拒绝处理 |
| 4 | 回到 Dashboard 执行一次正常同步/查询动作 | 正常 Dashboard 功能不受影响 |

### 8.3 detail-probe 实机验证

| 步骤 | 操作 | 通过标准 |
|---|---|---|
| 1 | 在 Chrome 扩展页重载插件 | 新代码生效，无扩展加载错误 |
| 2 | 在内容工作台触发一轮监控详情补采 | 插件能接单并打开正确页面 |
| 3 | 查看工作台入库结果 | 评论、封面、作者、发布时间等关键字段不为空，失败项有明确原因 |

### 8.4 全链路真实采集 + 远程工位联调

| 步骤 | 操作 | 通过标准 |
|---|---|---|
| 1 | 小红书单篇采集 | 标题、正文、作者、互动数、封面完整 |
| 2 | 小红书批量采集 30 条 | 数量接近目标，不明显漏字段 |
| 3 | 抖音单条视频采集 | 标题、作者、互动数、封面/视频字段完整 |
| 4 | 抖音评论采集 | 评论数量、作者、内容、点赞数完整 |
| 5 | 工作台下发远程任务 | 插件能接单、执行、回写、完成 |
| 6 | 工作台停止任务 | 插件停止执行，任务状态正确回写 |

## 9. 验收结论模板

| 链路 | 结论 | 卡点 |
|---|---|---|
| 小红书采集 | 通过 / 有条件通过 / 不通过 |  |
| 抖音采集 | 通过 / 有条件通过 / 不通过 |  |
| 远程工位 | 通过 / 有条件通过 / 不通过 |  |
| 数据回写 | 通过 / 有条件通过 / 不通过 |  |
| 用户界面 | 通过 / 有条件通过 / 不通过 |  |

最终结论只回答一个问题：这个插件现在能不能稳定承担内容工作台的采集执行端。
