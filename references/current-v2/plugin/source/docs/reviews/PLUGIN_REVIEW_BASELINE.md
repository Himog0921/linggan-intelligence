# 灵感爆爆爆插件审查基线报告

> 审查日期：2026-05-07  
> 审查范围：当前本地工作区，只审查不修复  
> 审查人：Cindy + 执行策略

## 1. 项目状态

| 项目 | 结果 |
|---|---|
| 项目目录 | `/Users/moglenny/proma/选题插件-打磨中/linggan-boom` |
| 插件类型 | Chrome Extension Manifest V3 |
| manifest 版本 | `2.0.1` |
| package 版本 | `2.0.1` |
| 核心定位 | 内容工作台的浏览器采集执行端 |

当前工作区已有未提交改动，本次报告基于这些本地改动审查。报告不代表线上已发布版本。

## 2. 基线命令

| 检查项 | 命令 | 结果 |
|---|---|---|
| 测试 | `node --test tests/*.test.mjs` | 通过 |
| 构建 | `npm run build` | 通过，有 3 个体积警告 |
| 运行时依赖安全 | `npm audit --omit=dev` | 0 漏洞 |
| 全量依赖安全 | `npm audit` | 5 个开发/构建链漏洞 |

## 3. 测试结果

| 指标 | 结果 |
|---|---|
| 测试文件 | 85 个 |
| 测试用例 | 288 个 |
| 通过 | 288 |
| 失败 | 0 |
| 耗时 | 约 2.0 秒 |

测试运行时有 Node.js 模块类型提示：项目 `package.json` 没有声明 `"type": "module"`，但源码使用 ES Module 写法。当前不影响通过，但会让测试日志变吵，也可能带来轻微性能开销。

## 4. 构建结果

构建通过，产物在 `dist/`，目录大小约 `1.2M`。

Webpack 提示 3 个体积警告：

| 警告 | 说明 |
|---|---|
| `content.js` 575 KiB | 超过推荐体积 |
| `content` 入口 760 KiB | 超过推荐体积 |
| `popup` 入口 251 KiB | 略高于推荐体积 |

这些不是阻塞错误，但说明后续如果插件加载变慢，应优先拆分 content 脚本或延迟加载非首屏能力。

## 5. 依赖安全

生产依赖审计为 0 漏洞，说明插件运行时依赖当前没有 npm 已知漏洞。

全量审计存在 5 个开发/构建链漏洞：

| 依赖 | 等级 | 来源 |
|---|---|---|
| `picomatch` | high | 构建链 |
| `postcss` | moderate | 构建链 |
| `serialize-javascript` | high | `copy-webpack-plugin` / `css-minimizer-webpack-plugin` |

本次只审查不修复，因此未执行 `npm audit fix`。这些问题不直接进入插件运行时，但属于发布链路风险，应进入后续修复任务。

## 6. 工具缺口

| 缺口 | 影响 |
|---|---|
| 没有 `npm test` 脚本 | 每次测试需要记住手动命令，容易漏跑 |
| 没有 ESLint 配置 | 无法用统一规则发现低级代码问题 |
| 没有覆盖率报告 | 只能知道测试通过，无法量化核心链路覆盖率 |

## 7. 基线结论

项目具备进入深度审查的基础：测试全过、构建通过、运行时依赖无漏洞。

但它还没有形成稳定的企业级验收门禁：缺少标准测试脚本、缺少 lint、缺少覆盖率、构建链有安全漏洞。
