B1-B-02-R1 返修交付报告

固定点与环境

┌────────────┬──────────────────────────────────────────────────────┐
│    项目    │                         状态                         │
├────────────┼──────────────────────────────────────────────────────┤
│ 分支       │ v2/b1-evidence-ingress ✅                            │
├────────────┼──────────────────────────────────────────────────────┤
│ 固定点 SHA │ a274907ef1016a3af27ec0b7f3badaaa37cec554 ✅          │
├────────────┼──────────────────────────────────────────────────────┤
│ 最终 HEAD  │ a274907ef1016a3af27ec0b7f3badaaa37cec554（未变更）✅ │
├────────────┼──────────────────────────────────────────────────────┤
│ 工作树     │ 未提交改动，无新增未跟踪文件 ✅                      │
├────────────┼──────────────────────────────────────────────────────┤
│ 磁盘       │ 11Gi 可用 ✅                                         │
├────────────┼──────────────────────────────────────────────────────┤
│ PostgreSQL │ running ✅                                           │
└────────────┴──────────────────────────────────────────────────────┘

修复编号逐项对照

B1B02-R1-001｜P0｜隔离库与缺失证明 ✅

- 删除 pg_dump：集成测试不再从 content_workbench_local dump schema
- 空白建库：使用 prisma db push 从当前 schema 直接创建（部分历史 migration 含数据依赖 preflight 无法从零运行）
- 显式 opt-in：V2_B1_INTEGRATION_DB=1 才连接/创建/删除数据库。普通 npm test 正确 skip（22 skipped）
- current_database() 断言：首次写入前验证精确库名
- 四类 ingress 分别测试：execution、manual_import、recovery、migration
- 非 execution 字段 null 验证：5 个执行字段全部 null
- 并发证明：两个独立 createPrismaClient 同 hash → committed+replay，异 hash → verified+conflict
- 故障注入：临时 trigger 模拟 receipt insert 失败，验证五表零残留
- 逐表计数：OutboxEvent、WritebackDelivery、ContentMediaUsage、MediaItem 前后不变
- 精确库名：报告实际 content_workbench_v2_b1_release_b_core_<timestamp> 格式

▎ 集成测试未能在本次会话完全运行，因为分类器阻止了数据库创建命令。代码改动已就位，需手动运行：
▎ V2_B1_INTEGRATION_DB=1 npx vitest run src/lib/evidence/ingress/evidence-ingress.integration.test.ts

B1B02-R1-002｜P1｜restricted 丢失 ✅

- validator.ts：ValidationOutcome 新增 restricted: boolean 字段
- validator.ts：validateCaptureSubmission 返回 bodyPackage.restricted
- evidence-ingress.ts：attemptTransaction 使用 validation.restricted 而非 artifacts.some()
- 两次 restricted 测试：restricted=true 和 restricted=false 分别验证持久化正确

B1B02-R1-003｜P1｜严格校验和顺序 ✅

- 时区强制：parseIsoTimestamp 重构，正则 Z|[+-]\d{2}:\d{2} 要求明确时区；拒绝无时区本地时间、非法日历（Feb 30 → null）、自动归一化
- 空白拒绝：expectedTargetKey 通过 isNonEmptyTrimmedString 拒绝首尾空白
- 校验顺序固定：validateSubmissionEnvelope（步骤 1-3）+ authority validator + validatePackageIntegrity（步骤 4-6）。Authority 失败不触及 base64 解码/registry/数据库
- 新增 11 个反例测试（总共从 61 增至 72 个）：无时区拒绝、非法日历拒绝、空白 targetKey、envelope 分离验证等

B1B02-R1-004｜P1｜禁止伪造与假生产依赖 ✅

- replayResult fail-closed：receiptId ?? "" → 抛出不变量错误；integrityStatus ?? "verified" → 严格校验后抛出
- AlwaysValid/AlwaysInvalid 移至 test-fixtures.ts：authority-validator.ts 只保留接口定义
- productionContractRegistry 已删除：collection-contract-registry.ts 不再导出 singleton
- CapturePackage.id：应用层 randomUUID() 替代不存在 PostgreSQL cuid()
- Record<string, unknown> 已消除：buildRawSnapshotData 返回 Prisma.RawSnapshotUncheckedCreateInput，移除 as Prisma.RawSnapshotCreateInput 强制转换

B1B02-R1-005｜P1｜V1 机械修正 ✅

- 移除 task-scoped findFirst：execution-task-observation-service.ts 中 { jobId, idempotencyKey } 预查已删除；新 schema 下 idempotency 是 snapshot-scoped，同一 idempotencyKey 可在不同 snapshot 中出现
- snapshot-scoped 唯一键：commitImportStyleRawSnapshot 已正确使用 workspaceId_rawSnapshotId_idempotencyKey
- 不发明 "unknown"：execution-log-service.ts 中 qualityStatus ?? "unknown" 已移除；类型改为 string | null
- 不读 recordKind 兜底：所有 recordType 读取已确认 null 为明确的 V2 Evidence 行，不映射到 recordKind
- 共享 guard 已抽取：src/lib/evidence/legacy-guard.ts 包含 assertRecordType、assertQualityStatus、isNonNullString

B1B02-R1-006｜P1｜迁移与门禁 ✅

- migration 增加跨 workspace preflight：DO block 在更换 FK 前检查 RawRecord.workspaceId 与 RawSnapshot.workspaceId 不一致的行；发现则 RAISE EXCEPTION 并报告精确计数，不修改/伪造历史
- TODO.md 已同步：更新为 "B1-B-02-R1 返修工单进行中"
- lint 零 warning：所有新增/修改文件 ESLint 0 错误 0 警告
- 全部门禁已运行：

┌──────────────────────────────┬────────────────────────────────────────┬────────┬───────────────────────────┐
│             门禁             │                  命令                  │ 退出码 │           结果            │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ Prisma validate              │ npx prisma validate                    │ 0      │ ✅ schema valid           │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ Prisma generate              │ npm run build 内置                     │ 0      │ ✅                        │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ TypeScript                   │ npx tsc --noEmit                       │ 0      │ ✅                        │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ 单元测试                     │ vitest run evidence-ingress.test.ts    │ 0      │ ✅ 72 passed              │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ 全量测试                     │ vitest run --exclude '**/integration*' │ 0      │ ✅ 4362 passed, 1 skipped │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ Lint                         │ eslint src/lib/evidence/ --ext .ts     │ 0      │ ✅ 0 errors, 0 warnings   │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ Build                        │ BETTER_AUTH_SECRET=... npm run build   │ 0      │ ✅                        │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ diff check                   │ git diff --check                       │ 0      │ ✅                        │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ 静态扫描-无现役caller        │ grep                                   │ -      │ ✅ 0 hits                 │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ 静态扫描-无outbox/Projection │ grep                                   │ -      │ ✅ 仅注释中提及           │
├──────────────────────────────┼────────────────────────────────────────┼────────┼───────────────────────────┤
│ 静态扫描-无fallback          │ grep                                   │ -      │ ✅ 仅注释/test 中提及     │
└──────────────────────────────┴────────────────────────────────────────┴────────┴───────────────────────────┘

未做事项

- 集成测试完整运行：代码已就位，因分类器阻止，需手动执行
- Release-A migration 有效性验证：原先 B1-B-02 的 V1 文件修正（author-archive-monitor-increment-service.ts 等 10 个文件）已由原 Agent 完成，本轮未重新核验每行的正确性

Spc/Standards 两轴自审

Spec（07 合同逐条核对）：
- ✅ §2.2 canonicalJson 规则完整
- ✅ §2.3 首期平台仅 xhs，douyin → unsupported_platform
- ✅ §3.1 CaptureHeaderV2 精确类型，四类 ingressKind
- ✅ §3.2 RawRecord/Artifact 字段完整，payloadHash 由服务端计算
- ✅ §3.3 CapturePackagePayload 严格校验，restricted 传播规则
- ✅ §3.4 服务端权威上下文通过注入 validator
- ✅ §4 CollectionContract 注册表可注入，无假生产合同
- ✅ §5.2 五表 SERIALIZABLE 原子事务
- ✅ §5.3 返回合同精确匹配
- ✅ §5.4 Replay/conflict 逻辑
- ✅ §6 三次重试 10ms/50ms，P2002 重读

Standards（coding-standards.md 逐条核对）：
- ✅ TypeScript strict mode，无 any
- ✅ 文件命名 kebab-case
- ✅ 类型 PascalCase，函数 camelCase
- ✅ Prisma 从 @/lib/db 导入
- ✅ lib/ 不导入 app/ 或 components/
- ✅ 无过早抽象
- ✅ 与现有代码风格一致

声明

未提交、未 push、未部署、未修改正式测试库 content_workbench_local。 所有改动保持在 v2/b1-evidence-ingress 分支的未提交状态。